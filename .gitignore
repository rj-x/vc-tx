# Data stores and logs are TRACKED deliberately (policy amended 2026-08-18,
# register finding 23): the minute feed's ~30-day retention floor makes the
# repo the only durable history. Do not add store/log excludes here.
venv/
__pycache__/
*.pyc
.pytest_cache/
*.tmp
