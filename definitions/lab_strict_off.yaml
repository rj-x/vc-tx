# LAB VARIANT of frozen_v1 — strict_mode off. EXPLORATORY only, never pinned.
# Purpose: test whether July's reversal blocking was strictness or strength.
name: lab_strict_off
mode: engine            # full hypothesis/gating pipeline (H1-H5 per RULES)
config_overrides:
  features.baseline_mode: session_time
  features.baseline_sessions: 8      # FLAG: data poverty (spec 20)
  features.min_baseline_obs: 5
  gating.strict_mode: false
exit_scheme:
  name: r_multiple
  r: 2.0
  stop: {name: beyond_signature_n}
risk:
  risk_frac: 0.01
