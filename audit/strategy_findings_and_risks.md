# Strategy Findings & Risk Register

Companion to `vsa_strategy_prompt.md`. Captures known weaknesses, improvement ideas, and open questions so they survive across sessions and iterations. Update this document as items are resolved or new risks surface.

---

## 1. Critical risk: look-ahead leakage (ADDRESSED IN v1 SPEC — remain vigilant)

**History:** prior backtesting projects built with AI assistance produced strategies that only appeared to work because future information bled into decisions. This is the project's #1 failure mode.

**Defenses now in the spec** (Non-Negotiable section of the prompt): clock-gated data access layer; explicit bar timestamp convention; truncation-equivalence testing of precomputed features; automated future-perturbation test over random cutoffs; explicit checklist of subtle leaks (full-sample normalization bins/percentiles, unconfirmed swings, HTF state before close, levels defined with later data); leakage tripwire — exceptional results (PF > 2.5, win rate > 65% at ≥ 2R, near-monotonic equity) are presumed leakage until the perturbation test clears that exact configuration.

**Residual risk:** leakage via the *iteration process itself* (see finding 3) and via any manual data handling outside the engine. Never hand-inspect lockbox data.

## 2. Volume data quality (OPEN — first-iteration diagnostic)

Everything rests on Finsa tick volume being a faithful proxy for real participation. This is assumed, not tested.

**Action:** add a volume-quality study to the first iteration's diagnostics: (a) does tick volume show the expected intraday shape (open/close surges, lunch lull, 14:30 London jump on FTSE/DAX)? (b) does it spike on known macro release timestamps? (c) if a sample of real FTSE futures volume can be exported from any broker platform, correlate the two over the same period — this quantifies how degraded the absorption signal is.

**Status:** open. Depends on step-zero verification confirming a volume field exists at all (hard gate).

## 3. Iteration = quiet overfitting (ADDRESSED IN v1 SPEC)

Each review-refine-rerun cycle fits the rules a little more to the same history. Data snooping by another name.

**Defense adopted:** lockbox — most recent ~20% of 1M history (min 4 trading weeks once available), excluded programmatically at the store/loader level, evaluated exactly once when iteration is declared complete. New data accrues to the lockbox between formal iteration rounds.

**Discipline required from us:** the lockbox verdict is final for that iteration round. If it fails, the strategy failed — the answer is not to iterate against the lockbox.

## 4. Thin statistical power early on (ADDRESSED IN v1 SPEC)

Intraday-only + one instrument + gated hypotheses + short accumulated 1M window = possibly ~30 trades in the first backtests, which proves nothing either way.

**Defense adopted:** event-study layer — forward mid returns at +5/+10/+20 Signal-TF bars after every confirmed hypothesis, independent of gating/sizing/EOD/trade simulation, vs. matched baseline bars. ~10× the observations; answers "do the signatures predict anything?" while trade samples mature.

**Interpretation rule:** event-study significance without trade-level profitability means the edge exists but is being eaten by costs/EOD/spread — a fixable problem. Trade-level profit without event-study significance is suspicious (luck or leakage).

## 5. Cliff-edge thresholds (DEFERRED — v2)

Labels are boolean (volume 1.49× baseline isn't "high", 1.51× is), but conviction is continuous. Binary labels waste information and make results threshold-sensitive.

**v1 mitigation:** ±30% sensitivity sweeps already in spec — edge must degrade gracefully.
**v2 candidate:** continuous signature scores (e.g., logistic transforms of rel_volume/rel_spread/close_pos) feeding hypothesis strength directly, replacing label counting.

## 6. Spread vs. stop distance on low TFs (OPEN — first-iteration diagnostic)

On 1M/3M executions, CFD spread may be a large fraction of a tight stop — an apparent 0.3R edge can vanish entirely into the spread.

**Action:** diagnostic reporting average spread as % of stop distance, per hypothesis and per session phase. If it exceeds ~10–15% for a hypothesis, that hypothesis needs wider stops or a higher execution TF.

## 7. Scheduled news contamination (PARTIAL — tag in v1, filter in v2)

A 13:30 London volume spike on CPI day is calendar, not psychology — it will generate false climax/absorption signatures. Session-time normalization cannot catch it (date-specific, not time-of-day-specific).

**v1:** tag all trades and hypothesis spawns within ±15 min of major scheduled releases (manually maintained list is fine initially) so contamination is visible in diagnostics.
**v2:** economic calendar integration as a spawn filter.

## 8. Other v2 candidates (parked)

- Volume profile / higher-quality supply-demand zones as the Layer-1 level source (upgrade path explicitly anticipated in spec).
- Overnight holding with gap-through-stop modeling.
- Trading divergence-migration across timeframes (log-only in v1).
- Reverse-and-flip on opposing confirmations.
- Live narrative mode: same engine fed by scheduled `collect.py` syncs, emitting per-TF phase, open hypotheses with scores, and alignment each minute (Part 10 / path-to-live discussion).
- Futures data with true volume, if Finsa instrument IDs for futures exist.

---

## Open questions (blocking or near-blocking)

1. **Does the Finsa feed / clean store contain a volume field, and what does it represent?** Hard gate — no engine work until answered. (Step zero.)
2. **How much 1M history is already accumulated per instrument?** Defines the initial backtest window and lockbox feasibility. (`collect.py status`.)
3. What interval is `quarter`, what timezone are timestamps, is there any pagination beyond `l`, and what is the true `l` limit? (Step zero.)
4. Do futures instrument IDs exist on Finsa? (Owner to ask provider.)
5. Do the existing scripts have any bugs affecting already-collected data (timezone drift, silent gaps)? If so, does historical data need re-validation? (Step-zero audit; report before fixing.)

## Decision log (headline design rulings)

- Intraday only; force-flat before cash close; entry embargo 30 min before cutoff. Overnight = v2.
- Intrabar stop/target ambiguity resolved by replaying constituent 1M bars; stop-first if a single 1M bar touches both.
- Execution TF fixed to 1M/3M; embargo overrides refinement.
- Gating is per hypothesis class; H3 range-boundary breakouts permitted in RANGING (`H3_RANGE_BREAK`); H5 exempt with ATR-extension gate.
- Ablation study (location / gating / confirmation independently disabled) mandatory, run on frozen config.
- Portability protocol: tune on FTSE, run frozen config on DAX and NASDAQ; per-instrument re-tuning reported separately.
- Engine reads only from the clean store (`store.py build`); scripts' output paths refactored to project root with equivalence proof; audit before refactor; report data-affecting bugs before fixing.

---

## Pseudocode review rulings (v1 restatement — logged so nothing lives only in chat)

**Rulings on Claude Code's [A1]–[A17]:** signature ownership rule (climax→H1, UPTHRUST/SPRING→H2, no label spawns two specs — resolves [A1] and its long-side twin); `CONFIRMED_PENDING_GATE` state replaces re-confirmation ([A2]); `EFFORTLESS_DECLINE`/`VALIDATED_DECLINE` added ([A3]); TEST additionally requires volume < 0.5× signature bar rel_volume ([A4]); concurrency rules accepted + blocked-spawn logging ([A5]); [A6]–[A17] accepted as proposed, with [A10]'s near-no-op base/threshold and confirm-bar-delta-before-check documented as deliberate, and [A15]'s pullback volume slope logged for later evaluation.

**Second pass:** structural-core vs. context-qualifier label rule (confirmations reference structural core only — fixes unreachable NO_DEMAND confirmations in H5/H2); refinement invalidation on parent refutation (`REFINEMENT_CANCELLED_REFUTED`), single pending refinement; H3 direction rule for RANGING Signal TF (out of range from the absorption boundary); H1 spawn-level definition under new-low spawns; H4/H3 stops computed at graduation; inclusive lowest-low lookback.

**Forensic pass:** **CRITICAL spec bug — reversal gate lacked a phase-agreement branch**, blocking the framework's canonical trade (upthrust in Context MARKDOWN; climax ending a pullback in Context MARKUP). Fixed: agreement branch first, tagged `REV_WITH_TREND` for separate reporting. Also: descending-TF processing order at simultaneous closes (determinism + perturbation-test stability, unit-tested); H3 zone grows until confirmation, breakout beyond max(level, zone edge); H4 refutation = expansion as pullback state + structural break as trigger; minor mirror/tolerance/lookback fixes.

**New requirement:** synthetic-scenario verification before any backtest (named cases including `REV_WITH_TREND` and `CONFIRMED_PENDING_GATE` paths), reviewed against the psychology appendix.

**Process lesson:** each review layer (document → instantiation → runtime trace) found bugs invisible to the previous one; the critical gating bug survived four document reviews and was only exposed by tracing runtime state. Next bug class lives in execution — hence the synthetic scenarios.

---

## RULES.md v2 review rulings (2026-08-11)

**All six reviewer findings verified and ratified:** R1 CPG pending window (`pending_gate_max_bars`, default 2× Context/Signal ratio, replaces confirm-window expiry once confirmed — prompt patched); R2 CPG skips strength machinery entirely, floor kill never applies in CPG (prompt patched); R3 two-phase step (evaluate all → resolve conflicts by strength → act once); R4 in-position guard on all entry paths + direct-entry restatement when refinement disabled; R5 evidence semantics — general definition added to prompt, per-hypothesis enumeration required in RULES.md v3; R6 H5 extension gate signed in the fade direction (spec bug, prompt patched). All implementation traps accepted: H3 age re-anchors on each qualifying cluster bar; "outermost in trade direction" wording; H4 pullback boundaries pinned (start = REACTION transition, mean excludes evaluation bar, low at graduation); `zone_level` = the key level Lv; refinement window bar 1 = first exec bar closing strictly after graduation; triggered entry whose fill would land inside the embargo → abandon.

**The three referred rulings:** (1) opposite-direction graduation during pending refinement → **cancel the pending entry** (`REFINEMENT_CANCELLED_OPPOSED`); the opposing hypothesis may begin its own refinement — no position was open, so this is not reverse-and-flip; oscillation bounded by expiry, logged for diagnostics. (2) Confirm-while-understrength → **log-and-lose** for v1 with a `CONFIRM_UNDERSTRENGTH` counter in diagnostics; revisit if the counter is material once thresholds are tuned; no third pending state in v1. (3) `pending_gate_max_bars` default **2× Context/Signal ratio** ratified.

**Spec-bug tally:** two critical defects have now originated in the spec itself (unsigned H5 gate; missing reversal phase-agreement branch) — the spec is not privileged over the restatement; both documents are subject to the same adversarial process.

## Step-zero data findings (from DATA.md — headline implications)

- **Volume is real futures contract counts** (`uk100fut`, ID 70152), not tick volume — the hard gate cleared in the best possible way; absorption hypotheses run undegraded. Cash CFD volume = same series ×4: use ONE series (futures) as canonical; never mix.
- **URGENT operational requirement: rolling ~30-day provider retention.** Data older than the window is unrecoverable. Scheduled sync (daily; hard ceiling every 3 weeks) is now infrastructure-critical — every missed window is permanently lost backtest data.
- `uk100fut` is a RAW SPLICE at quarterly rolls — roll-gap handling (prompt Part 6) applies; signature exclusion around roll transitions; Sep-26 expiry watch item.
- DST session-label bug on US/gold instruments: must be fixed before the NASDAQ portability stage; does not affect FTSE development.

---

## Backtest campaign log

**2026-08-12 — Backtest v1 (first real-data run; report: `reports/backtest_v1.md`).**
Outcome: **sample starvation** — 16 working-set sessions (exactly 5 consumed by warmup), 2 spawns / 1 confirmation / 0 gated trades on uk100fut (uk100 secondary consistent: 6/2/0). No tuning performed; lockbox untouched. **Compromise in force: `baseline_sessions` 8 / `min_baseline_obs` 5 vs spec defaults 20/20** — all results conditional on this until data allows spec values. The single gate-veto observation (ablated no-gating trade lost 1.04R with negative forward returns at all horizons) is **explicitly non-evidential (n=1)** and is recorded only because the ablation table requires it. New standing diagnostics: per-TF label denominators with conservation-checked spawn fates; label-level event study (primary powered readout for the accumulation window); spread-vs-stop (first reading ≈31% of stop on the one ablated trade — above the 10–15% alarm line in finding 6, thin-sample flagged). Cost-model gap found and fixed during amendment: slippage now applies to entry fills, not just exits. DST session fix (finding: US/gold in_cash now computed in America/New_York) shipped; macro-release calendar scaffolded (`data/macro_releases.csv`, empty — owner to populate). Next checkpoint: first powered label-level readout or walk-forward feasibility, whichever first.

## Standing rulings (2026-08-12 sign-off)

1. **EFFORTLESS_DECLINE excess signal — tracked, not acted.** The −19 bps drift-adjusted wrong-way reading at n=16 is a tracked observation only. If it survives to powered n, it enters the system **only through walk-forward as a candidate hypothesis** — no mid-accumulation rule or threshold changes on its account. (Ruled 2026-08-12; guards the quiet-overfitting defense in finding 3.)

2. **OPEN QUESTION — refinement stop-tightening vs spread burden.** Does execution refinement's R-geometry gain pay for its proportionally larger spread cost? Current evidence (corrected 2026-08-12 — the initially quoted 31% was a hand-derivation error; programmatic figures from logged fill-to-stop): spread ≈ 2.6% of the signature-stop distance (~50 pts) vs ≈ 10.8% of the exec-local tightened stop (12.0 pts) — a ~4× spread-burden ratio, both below the 15% alarm line, single observation each. Weekly campaign reports carry the graduation-stop distribution **split by stop basis** (signature at every graduation, all variants; exec-local at every refined entry) so both populations accrue; resolved by the spec-mandated with/without-refinement comparison once trade samples are powered.

3. **Reports are generated, not written** (effective 2026-08-12, extracted from the 31%-figure correction). Every quantitative claim in any report must be programmatically derived from logged artifacts; hand arithmetic is banned. Implemented: `backtest/report.py` renders `reports/backtest_v1.md` entirely from `reports/backtest_v1/*.json` (trade records now persisted per variant for this purpose); the campaign regenerates the report on every run; the file carries a GENERATED banner. Interpretive prose may carry no hand-computed numbers.


4. **Macro calendar scope contract** (2026-08-12). `data/macro_releases.csv`: timestamps/currency only, red-impact only by construction; used for ex-post tagging (±15 min) and the macro-spike volume check exclusively; **never engine-facing without clock-gating** (actuals are post-release information). Validation-on-load is mandatory for every consumer (`engine/macro.py`: schema, Z-suffix UTC, monotonic, NFP 12:30/13:30 anchor — hard fail). Forward population: `scripts/macro_fetch.py` (FairEconomy JSON feed; HTML scraping banned) writes `data/macro_releases_staging.csv` only; the human merge gate into the live file is deliberate and stays. Deferral: **surprise-conditioned release analysis (actual vs. forecast) — v2 candidate, backfillable from official archives, no capture urgency.**

5. **narrate is diagnostic/non-evidential** (2026-08-13). `engine/narrate.py` (replay + live) is the human front door to the narrative engine: labels/phases/hypotheses/dump only, always narrative-only past the lockbox boundary (scoped loader access, logged on every use; refused without the explicit flag). **No thresholds, rulings, or tracked signals may be touched off any narrate output.** Live mode is v2 machinery arriving early: polling watcher on the collector's rate discipline, always narrative-only, and must not displace the weekly cadence. First replay exercised 2026-08-13 05:00-09:40Z uk100fut (reports/narrate/). Note: that replay printed an EFFORTLESS_DECLINE at the session low preceding the rally - consistent with the tracked signal (ruling 1), explicitly non-evidential, n=1.


## v2 parking lot (additions)

- **Extended-hours signature detection** (2026-08-13): the 2026-08-13 06:56Z Asia-high rejection occurred 4 minutes pre-cash-open and is structurally invisible under cash-only detection; session-model implications unscoped.

- **Basis mechanics note** (2026-08-13, diagnostic): uk100fut-vs-uk100 close basis is intraday-stable (2026-08-13 morning: mean +32.2 pts, std 0.19 over 278 min) and steps on FTSE ex-dividend Thursdays (+~20 pts 2026-08-06, +~30 pts 2026-08-13; cash drops the dividend, the future does not), drifting with rates-minus-remaining-dividends between. Narrate snapshots confirmed faithful. Relevant to any future cross-series logic (basis-step days must never read as price signatures on a fut/cash spread).

6. **Execution vehicle pre-registered (Part A, 2026-08-13, BEFORE any powered results).** Deployment reality modeled exactly: signals/levels/stops/R on uk100fut; execution on the uk100 cash CFD — fills at measured bid/ask (long entry@ask exit@bid, mirrored), intrabar resolution on uk100 1M, GBP-per-point sizing with configurable minimum stake (SKIPPED_SIZE retained), no commission, no roll on the execution leg. Stop/target levels mapped fut->cash by basis-at-entry; justification on record: intraday basis stability (std 0.19 pts, 2026-08-13) + EOD-flat asserted as a config precondition (broker refuses cash_cfd without an EOD hook). Spread reported per session-time bin from real quotes; open question R2 now tracks measured uk100 spread. Old-cost-model report snapshotted once (reports/backtest_v1_futures-direct_final.md) for continuity; thresholds and lockbox untouched.

7. **Extended-hours observational instrumentation (Part B, 2026-08-13; evidential path untouched).** 24h session-time bins on futures trading-day boundaries (~21:30 London anchor) with per-bin observation counts reported (undercooked bins visible); structural labels/phase/levels run all hours, every emission segment-tagged (overnight_asia / pre_open 07:00-08:00 London / cash / post_close); hypothesis machinery outside cash is STRUCTURALLY narrative-only (SIGNAL_EXTENDED_OBSERVATIONAL — hypotheses spawned outside cash can never trade even if confirming in cash). Trading pipeline unchanged: spawning-for-trading, gating, and campaign trade metrics remain cash-session-only under the frozen config. Weekly readout gains the drift-adjusted label event study split by session segment — the experiment on whether volume signatures rescale in thin sessions. narrate/live show extended hours by default with segments visible. **Extended-hours results feed no thresholds or rules before walk-forward.** Motivating case: 2026-08-13 06:56Z Asia-high rejection.

8. **Late-session swing confirmability** (2026-08-13, spec gap; ruling to follow). Code today: swing confirmation carries ACROSS the session boundary — the 2k+1 fractal window is a rolling deque that never resets, so a swing in the final k bars confirms only when the NEXT session's opening bars arrive, with fractal neighbors spanning the overnight gap. Worked example: 2026-08-13 14:45-15:15Z range-break lows (10801.8->10785.8) — the only range break of the day — sit inside the final k bars and were unconfirmable at the close; RANGING verdict stood on the confirmed sequence (11:00Z higher high included).

9. **Day-relative vs time-of-day-relative signature detection** (2026-08-13, named question; no changes now). The 06:45Z rejection bar: 98th-percentile spread vs trailing-50 bars, 15th vs its session-time bin — the two normalizations disagree by construction in quiet segments. Adjudicated by the segment-split event study; feeds no thresholds before walk-forward.

10. **Execution-TF classification gap** (open). v1 runs the classifier/context pipeline on Context+Signal TFs only; 1min feeds refinement/broker but is never classified, so narrate --show-tf 1M shows no labels. Spec Part 5 says identical pipeline per TF — known v1 deviation, prioritization pending.

**Rulings closing items 8 and 10 (2026-08-13):** Item 8 RATIFIED as spec — cross-boundary k-window confirmation stands (formalized in prompt Part 3); swings tagged confirmed_across_gap with a weekly diagnostic count; **EOD-provisional confirmation registered as the named v2 alternative** if tagged swings prove pathological. Item 10 CLOSED — classification extended to the 1min TF as OBSERVATIONAL instrumentation: separate drift-adjusted segment-split 1min label event study (uk100fut_exec_label_study.json, autocorrelation caveat embedded, never pooled with 15min), narrate --show-tf 1M live, no threshold or rule consumes 1min labels before walk-forward, formal checkpoint remains Signal-TF. Compute impact measured: full campaign 29s wall (was ~20s), primary narrative artifact 3-4MB — immaterial to the sync-to-report cadence; no sampling scheme needed at current scale.

11. **Candidate-hypothesis register** (2026-08-13): `audit/candidate_hypotheses.md` pre-registers H6 (session-extreme rejection, day-relative baselines, volume-agnostic) and H7 (quiet-decline reversal at session extremes — the formal walk-forward entry for the tracked EFFORTLESS_DECLINE signal per standing ruling 1). Status CANDIDATE: not implemented, not consuming data, walk-forward entry only; the file is append-only with a logged amendment trail. Provenance: items 8-10 and ruling 5. Guard: `tests/test_candidate_guard.py` greps engine/ and backtest/ for the identifiers and fails the suite on any reference before walk-forward.

**Meta-history note (2026-08-13, cross-ref item 11 / candidate-file amendment 1):** first instance of implementer methodological pushback changing a ruling — the candidate-file design adjudication: dissent -> counter -> synthesis, outcome (frozen anatomy / free parameters) stronger than either original position.

**Field-review annotations, 2026-08-11 ledger session (review-surface, non-evidential):**
- 10:27/10:29Z 1min UPTHRUST prints @ ~10865: anatomy-correct, LOCATION-ABSENT (14 pts below session high), failed forward (rallied to 10880) — concrete instance of anatomy-without-location noise; supports the hypothesis layer's location requirement (cf. ablation (i)).
- 15:47Z 1min UPTHRUST: anatomy-correct, worked (7-pt risk, 37-pt run, stop untouched), post_close segment — **extended-hours exhibit #2** alongside 2026-08-13 06:56Z (v2 parking lot, extended-hours signature detection).

**Item 11 addendum (2026-08-13):** H8 (Signature-Moment Expansion Bracket, direction-agnostic class) registered in two-layer form; guard extended to H6/H7/H8. Evidence instrument built: expansion event study (uk100fut_expansion_study.json — forward realized range + up/dn excursions at +5/+15/+30 1min bars vs same-segment baselines, location splits from the ledger's dist_signal_atr/location_ref, measured spread stated alongside; observational, working set only). Motivating cases = the two 2026-08-11 field annotations, observation-only, no outcome computation on post-boundary data.

**Item 11 / H8 addendum — standing evidence status (2026-08-14, matched-baseline first reading):** "early evidence adverse: anatomy residue +8% at extremes net of volatility matching; double-trigger 0.57-0.66 at minimal buffer; spread 0.8 — bracket viability doubtful, directional variants unaffected by this result." Non-evidential; verdict at walk-forward. Cross-reference: the overnight_asia at_extreme cell (−4.2% net of matching) is logged as **item 9's first data point** — signature prints under thin-session volatility matching underperform their matched baseline, bearing directly on day-relative vs time-of-day-relative signature detection. Guard-circumlocution note: `backtest/expansion.py`'s prose reference to "the candidate register's signature-moment expansion bracket entry" (rather than the identifier) is **deliberate and load-bearing** — the H6/H7/H8 guard (`tests/test_candidate_guard.py`) scans that file; do not add candidate identifiers to engine/ or backtest/ before walk-forward.

12. **TF-ladder + migration instrumentation and H9** (2026-08-14). Scope contract: observational throughout — ladder TFs (1min/3min/5min/15min/30min/1h) run classifier+context only, no hypothesis manager, structurally (item-10 scoping); only the configured stack holds hypothesis/gating roles; every emission segment-tagged; per-TF label studies never pooled (nesting caveat embedded: higher-TF labels are compositions of lower-TF bars, not independent confirmations); migration events (persistence >= N same-direction child labels in one parent bar + same-direction parent close + RECRUITMENT: parent rel_volume >= floor) logged with chains, timing, recruitment margins; weekly migration study reports recruitment-passing vs -failing separately. **Standing rule: ladder/migration outputs feed no thresholds or rules before walk-forward.** Evidential path untouched (ladder off in all trading variants). H9 (Timeframe-Pressure Migration, direction-follows-pressure class) registered two-layer in the candidate file; guard extended to \bH[6-9]\b; migration code references the candidate by prose per the load-bearing-omission convention. **Recruitment-condition design lineage (logged as design rationale): implementer tautology objection -> owner participant-depth reframe -> recruitment synthesis** — the falsifiable second clause of H9's frozen claim exists because of this exchange. Compute impact: campaign 35.6s (+12s with ladder on the observational run only), migration artifact 125K, ladder studies 58K — immaterial; per-TF toggles exist (session_model.ladder) if scale changes.

**Item 12 addendum (2026-08-14, verification + first readout):** Evidential-path invariance across the ladder build is now a TESTED FACT: (a) one-off diff — post-ladder evidential outputs (trade record to the penny, 374/58 label denominators, 2-spawn fate table) match the pre-ladder documented values (pre-ladder JSONs were overwritten in place; the generated-report values served as baseline — noted as a reason to start committing artifact baselines); (b) standing regression `tests/test_ladder_invariance.py` — ladder-on vs ladder-off runs on the same store must produce identical evidential event streams and trade records (covers the ClockGatedFeed modification). Migration first readout logged: **direction-consistent with the candidate's frozen claim (cash rungs-1 recruited +3.5 bps @5 vs unrecruited −3.1), magnitudes non-evidential at current n.** Cross-file: the post_close recruited cell (−12.8 bps @5) is **item 9's data point #2** — thin-session behavior diverging from cash again, now on the migration surface.

13. **Version control adopted as standing discipline** (2026-08-14). Provenance / motivating gap: the ladder-build invariance one-off had to diff against values that survived only in report prose — pre-ladder artifact JSONs were overwritten in place with no baseline. Tracked: code, config, docs, registers, and generated report artifacts (evidence, not build products); excluded: raw/clean stores and logs (large; reproducible via sync only within the ~30-day minute-feed retention — caveat in README). Rules (README operations): campaign artifact changes committed per run (week-over-week artifact diff = diagnostic); each register/candidate amendment is its own commit (append-only gains cryptographic dates); code commits per completed order. Initial commit 8cc9443 records: post-ladder, 37 green, config frozen-never-fitted, lockbox 2026-08-04.

**Item 13 — LOGGED AMENDMENT (2026-08-14, git archaeology; not a silent edit):** the original provenance ("no VCS baseline existed") was WRONG. Prior owner history existed (c85d068..b8e1095, 2026-08-10 -> 2026-08-14, including data snapshots and report artifacts) but was unverified by both reviewer and implementer. The gap was discipline-around-artifacts, not absence of VCS. Archaeology results: (1) DATA INVENTORY — all committed data snapshots are strict subsets of the current store (identical 1M start dates: uk100 2026-07-03, uk100fut 2026-07-12; the live store's union merge already contains every committed row): zero recoverable sessions beyond retention, no lockbox implications, no recovery plan needed. (2) BASELINE UPGRADE — pre-ladder artifacts exist in b8e1095 (2026-08-14 09:07); the invariance one-off is UPGRADED to a bit-level diff: all evidential variant JSONs (full_2r, abl_no_gating_2r, full_1r, strict_2r, uk100_full_2r) are byte-identical pre-ladder -> post-ladder.

**Meta-history addition (2026-08-14): repository-state claims join market-data claims under compute-don't-assert.** The premise of item 13 went unverified by both reviewer and implementer — "no commits exist" was asserted from a stale session snapshot and accepted without a `git log`. Same rule as everywhere else in this project: verify against the artifact, not the recollection.

**Ruling 4 amendment — capture-all, consume-filtered (2026-08-14, design rule).** Staging captures EVERY feed event (all currencies/impacts, + forecast/previous staging-only); the live file stays lean (utc_time,currency,impact,name); consumption filters through per-instrument `macro_relevance` config (uk100fut = {GBP,USD} x {High} — tagging behavior pinned bit-identical by test). Loader validates impact in {High,Medium,Low,Holiday}; NFP anchors unchanged; merge-gate view renders staged rows grouped by currency/impact. Notes: pre-change weeks hold GBP/USD-High only (backfill from official archives if another instrument's history ever needs more); **relevance filters are per-instrument config — a named walk-forward-adjacent question, not a constant.**

**2026-08-11 field annotation — LOGGED AMENDMENT (2026-08-14, not a silent edit):** the 10:27/10:29Z pair was "location-absent" only by the session-extreme reference (14 pts below session high); by the swing-registry reference it was location-PRESENT (−0.03 ATR from a registered level). The original annotation used the only reference then measured. **Reference-class discrimination (session extremes vs prior-session extremes vs swing-registry levels) is registered as an open question feeding H6's parameter schema**; the expansion study now splits by reference class, so the question is computable weekly.

**Week field-review scoreboard (w/e 2026-08-14; review annotations, non-evidential):** five verified events — 14.5R post_close winner; 2.2R-then-stopped at session high; three failures. Method note on the record: **discretionary recall surfaced the two winners; the ledger surfaced all five** — the review surface exists precisely to keep the failures in view.

**Week-of-08-10 register annotations (2026-08-14; non-evidential, narrative-only provenance):**
1. **H9 canonical worked example — 2026-08-12 12:29-13:30Z:** simultaneous multi-TF quiet-advance at the session high (EFFORTLESS_ADVANCE printing 1min/3min/5min/15min at 12:30, VALIDATED_ADVANCE 1min at the session high 10866), then a RECRUITED decline (VALIDATED_DECLINE at 1min/3min 12:36-12:38) propagating 1min -> 3min -> 5min -> 15min -> 30min -> 1h within one hour, ~43 pts to 13:30. Cross-reference: the recruitment condition's design lineage (item 12) — this is the pattern the participant-depth reframe predicted the definition would capture.
2. **First observed climax-and-test anatomy — 2026-08-11 11:02Z POTENTIAL_BUYING_CLIMAX** (first climax label in project history, 1min, at the weekly high 10878.6/swing 10879.1) **+ 11:24Z 3min TEST**, preceding a ~100-pt decline over the following sessions. Filed to the exec-TF/ladder studies as motivating texture (H1-anatomy at 1min scale; the Signal-TF has yet to print one).
3. **Scoreboard amendment (logged) — the 2026-08-12 12:31Z SPRING failure now explained as COUNTER-CASCADE:** it printed against the forming multi-TF decline of annotation 1 (long signature into a recruiting downward migration). Mechanistic support for MTF-alignment gating, n=1.

14. **Execution layer** (2026-08-14). **One engine, multiple feeds** — a strategy definition executes through the identical code path (classifier/context/hypotheses/gating/broker/exits) in lab, paper, and narrate; no mode-specific strategy logic anywhere; enforced by `tests/test_one_engine_invariance.py` (lab bulk path vs simulated-clock incremental path: bit-identical decisions and fills). **Exit universe pre-declared** for walk-forward: fixed_points, r_multiple, atr_k, beyond_swing_n, beyond_signature_n, trail_atr, trail_swing, breakeven_at_r (`engine/exits.py`; trailing tightens-only, same intrabar rules as stops). **Strategy schema** (`engine/strategy.py`, definitions/): engine mode + signal_rules composition (labels incl. ladder rungs, location by reference class, segment, phase, migration conditions); frozen config refactored to `definitions/frozen_v1.yaml` — campaign pin passed BIT-IDENTICAL; candidates referenced by prose per guard. **Zone model**: working (-> 2026-08-04, freely simulatable) | lockbox (-> go-live: sealed, one evaluation) | forward (go-live ->: paper-visible live only, never tunable, excluded from training like the lockbox); go_live_utc stamped one-shot into lockbox.json at first paper start (terminal boundary). **Lab** (`backtest.lab`): working-set-only, EXPLORATORY-stamped, physically separate artifacts, immutable trial log (hash/ts/metrics) = the multiple-comparisons record walk-forward MUST consult; graduation = candidate-backlog registration only. **Paper** (`engine.paper`): frozen_v1 only, zero strategy knobs, cash-CFD fills on live uk100 bid/ask, EOD/embargo enforced, FORWARD_PAPER append-only ledger, restart = warm+reconcile-close+logged coverage gaps (honest holes, never backfilled). **Deferred (named v2 option): lab strategies as live shadow ledgers.** Compute: suite 42 tests ~8s; campaign unchanged; paper smoke pending first supervised session.

**Drill-deviation note + rationale correction (2026-08-14, logged amendment style):** Outcome ACCEPTED AND PREFERRED — the standing synthetic drill test with shared-helper extraction beats the ordered one-off. **Rationale CORRECTED for the record: the implementer's stated justification ("real-data drills leak frozen-config behavior onto sealed data") is WRONG** — working-set data is not sealed; it is the freely-simulatable zone, and the weekly campaign already publishes frozen_v1's complete P&L over it by design. A real-data drill on July would have contaminated nothing. **The live-rehearsal ban applies to the lockbox and forward zones only.** Logged so no over-broad caution enters doctrine — false cautions metastasize like false permissions. (Meta-history: this register now holds both pushback that improved a ruling AND pushback whose outcome was accepted despite its reasoning.)

**Untested surface, accepted by design (2026-08-14):** real-feed latency under the live polling loop is the one surface the simulated-clock drill could not exercise; it receives its test at supervised go-live, where the failure mode is logged coverage gaps.

**GO-LIVE AUTHORIZED (2026-08-14):** owner starts the paper executor at a supervised moment, verifies the go_live_utc stamp, watches first polls, exercises one natural kill-and-restart, commits the stamped state. Standing expectation: first-session silence is the likely and correct outcome.

15. **GO-LIVE EXECUTED (2026-08-14).** go_live_utc = 2026-08-14T15:04:09.240421Z (ledger START +1s — stamp-then-begin, correct ordering). First session: two live APATHY labels validated against the chart; deliberate kill-and-restart exercised (1m36s honest hole, clean warm resume); ended flat. **Post-stamp checklist passed with evidence:** (1) zones read correctly — working set open (loader max ts 2026-08-03 23:59 < boundary), lockbox sealed both edges (2026-08-04 -> go-live), forward period unreachable by any tuning/training path (same loader mechanism as lockbox); (2) weekly report gained its FORWARD_PAPER section rendered from the live ledger; (3) campaign path unchanged (same default loader; artifact spans end 2026-08-03). **First-session items, dispositions:** (i) numpy leak FIXED at event construction (float() on location/high/low/close/rel_volume) + dual-path payload-type test; (ii) gap semantics FIXED forward to max(warm_through, go_live_utc); the first-session 6h25m entry stands with a clarifying NOTE appended to the ledger (pre-stamp lockbox-zone time = zone-irrelevant, not downtime); (iii) drill ledger renamed drill_ledger.jsonl + DRILL_BANNER first event (committed move); (iv) **FIRST-LIVE-TRADE VERIFICATION CHECKLIST, pre-registered — execute the moment the first FORWARD_PAPER ENTRY exists:** fill side correct (long@ask / short@bid); basis_at_entry sane vs that session's median basis; stop mapped via basis correctly; stake arithmetic verified to the penny against the definition's risk parameters; EOD/embargo interaction confirmed if applicable. The first trade is a test event; this checklist predates it.

**Item 15 addendum — persistence finding (2026-08-14, fixed for the Sunday restart):** steady-state polled bars were memory-only (catch-up warmed from the store; live polls fed the engine and wrote nothing — divergence inherited from narrate.live's diagnostic pattern, where persistence is deliberately out of scope). FIXED: persist-then-feed — every settled polled page goes through the standard collector merge (idempotent dedup, same code as sync) + clean rebuild for both legs BEFORE the engine sees the bars; warm-from-store now equals last-processed. Crash semantics as-built (pre-fix, on record): the memory-only span's DECISIONS are never reprocessed — restart warms fresh and processes only newly polled bars; the span's DATA re-enters via sync within retention, and on a later restart joins warm-replay CONTEXT only. Warm-replay caveat surfaced and guarded: the warm pass runs the full engine, so a simulated position could survive it — now cleared with a WARM_RESIDUAL_CLEARED ledger event (warm state is context only). Tests: steady-state polls advance both stores with dedup; warm point == last polled settled bar. Executor runs as-is through the weekend (exposure = re-fetchable Friday bars); Monday starts on corrected persistence.

**Meta-history, sixth entry in the compute-don't-assert line (2026-08-14, reviewer-logged):** during this diagnosis the reviewer initially misread the stores' open-time labeling as close-time and built a wrong theory on a one-row phantom gap; corrected in-conversation before reaching the implementer. **The standing rule now explicitly includes store timestamp conventions among the things verified before asserting** — the dual-convention line in narrate's header exists because this exact confusion is endemic; it caught the operator again anyway.

**Item 15 addendum — 16:10Z silence investigation (2026-08-14, non-evidential, forward-period diagnostics only).** WORLD A (executor stall), proven by contradiction: engine replay of the identical bar under the LIVE frozen baselines (8-session bins, min_obs 5) grades 2026-08-14 16:10Z-close EFFORTLESS_DECLINE — rel_spread_pct 100.0 (spread 4.0 vs bin [1.0..4.0], n_obs 8), close_pos 0.12 < 0.2, rel_volume 1.25 < 1.5 high-ceiling, down bar — all four conditions pass; one-engine invariance means a live executor processing that bar prints that label, so the silence indicts the process. Exact death mode INDETERMINATE from surviving artifacts (pre-fix session persisted nothing) — every death mode is now covered by: guarded() loud-death wrapper (EXECUTOR_ERROR to ledger+stderr, loop continues), WATCHDOG_STALL (5 quiet polls during expected-printing hours; calendar excludes the 21:00-22:10 London pause + weekends), both tested. Coverage hole noted in ledger; data loss nil. **Shadow-model calibration (the reviewer's reference procedure):** engine bins are per minute-of-trading-day anchored 21:30 London over the LAST 8 SESSIONS (not 24-session pools), percentile = midpoint rank vs prior bin values only (self excluded), volume ceiling for the effortless family = rel_volume < 1.5 (spec's "not high"), close_pos floor 0.2 — the 24-session shadow agreed here; where they diverge, recent-8 wins. **Cross-validation: engine features over 15:30-16:51Z corroborate the audit — 78% sub-median spread, 93% sub-baseline volume across 82 valid bars.** Sunday-restart set now: persist-then-feed, numpy floats, gap semantics, warm-residual guard, loud-death + watchdog.

**Item 15 addendum — live/replay divergence (2026-08-14).** 15:18Z/15:23Z bars: PRICES IDENTICAL live-vs-settled (H/L/C exact match both bars) — not a price race. Two mechanisms: (1) VOLUME SETTLEMENT RACE — live rel_vol 0.34 vs settled-replay 0.70 on identical prices; the drop-forming-bar rule is insufficient (the collector's own docs note the feed revises bars; volume accrues late). Fix (Sunday set): two-poll confirmation — a settled bar is accepted only when two successive polls agree on full OHLCV (adds ~60s decision latency, within the accepted latency floor). (2) WARM-STATE divergence on location fields (live session_low 10782.8 vs replay prior_session_low 10784.8) — extremes-tracker state depends on warm boundary; expected, not a bug, but must be visible. **Doctrine rule adopted: every live decision event embeds the exact bar it saw** (open/volume now added to labeled events — previously H/L/C only, which is why the volume race is unprovable retroactively) **and the weekly report gains a reconciliation check flagging live-event bars diverging from settled store rows.** (Reconciliation check queued; embed + confirmation landed.) **Corrections on the record:** the shadow-audit "silence correct" verdict overstated — recent-8 engine baselines are authoritative over pooled-24, and the settled replay shows the evening's true narrative was ~10 labels including a post-close effortless-decline ladder. **The dead witness swallowed the story; the operator's expectation of more narrative was correct three times over.**

**Item 15 addenda (2026-08-14):** (1) Friday's pre-fix live label events may carry UNSETTLED VOLUME — the settled-store replay is CANONICAL for that window. (2) The weekly reconciliation check (live-event bars vs settled rows) is PROMOTED from queued to prerequisite of the first-live-trade verification checklist — built ahead of need: the report's FORWARD_PAPER section now reconciles every ledger ENTRY's embedded bar data against settled store rows and flags divergences; currently 0 entries, check armed.

**Feed-contract amendment (2026-08-14): measure before fixing.** The index-0-is-forming behavior is operator recollection from prior Finsa work (possibly different endpoints/versions) — LEADING HYPOTHESIS, NOT FACT. `scripts/feed_probe.py` built: 10s polls over a 15-20 min window at Monday's cash open, raw pages logged UNTOUCHED (served order, no sort), answering (a) forming-bar index across minute boundaries, (b) same-minute volume accrual, (c) deeper-index post-settle revision (Friday's evidence conflated forming with revision; the probe separates them). Only after measurement: audit all three consumers' slicing, fix at the fetch boundary with a convention-pinning test, archive the capture as evidence, register the measured contract superseding both prior theories. Note: fetch_page sorts pages ascending before consumers slice, so the drop-last convention operates post-sort regardless of served order — the probe measures the served order AND the settle dynamics. Until then: Sunday restart proceeds with the current set — two-poll confirmation blocks the hypothesized failure mode under EITHER theory; Monday trades safely while Monday measures.

16. **Feature backlog — candidate sense-organs from OHLCV** (conceived 2026-08-15; ranked; observational instrumentation ONLY, built sequentially). (1) **Sequence velocity/acceleration + efficiency ratio** (net move / path length) — regime state the chop findings already demand; adjacent to the migration candidate's evidence. (2) **Approximate session volume profile + participation-weighted levels** — upgrades Layer-1's level vocabulary; feeds item 9's reference-class question. (3) **Contraction/coil sequences** — rival predictor for the expansion study. (4) **Cumulative signed volume** — running absorption line. (5) **Dwell-time at levels** — auction acceptance vs touch. (6) **Noted-as-unreachable: true order-flow delta and sub-1M path** — data ceiling; recorded so nobody rediscovers it. **Rules:** each enters as observational instrumentation feeding a drift-adjusted study (ladder precedent — segment-split, matched baselines where applicable, non-evidential stamps); hypotheses may only emerge from measured texture, pre-registered per backlog convention; nothing consumes these series as a threshold or gate before walk-forward; build order follows the ranking, ONE AT A TIME — next begins only after the previous produces a first readout. **Build #1 (velocity/efficiency) AUTHORIZED, slotted behind the Sunday restart, the Monday feed probe, and the reconciliation-check prerequisite.** Spec on record: rolling velocity (pts/bar over N), its first difference (acceleration), and Kaufman-style efficiency ratio over N, computed per stack TF, emitted as bar-context fields (available to narrate/ledger/studies); weekly study section: forward returns and signature-frequency conditioned on efficiency-regime terciles.

17. **Observer Cockpit — read-only web UI over existing artifacts** (registered 2026-08-15; build slot AFTER the measured-contract fix + reconciliation prerequisite; may interleave with velocity/efficiency — touches no evidential path). **Stack (operator-specified):** Preact + TypeScript, Vite, yarn; thin read-only Python backend (FastAPI, existing venv) serving session JSONL tail, paper ledger, heartbeat/watchdog stream, lockbox.json zones, clean-store bars — GET/stream only. **Structural fence, two places: the backend exposes no POST/PUT surface at all, and the frontend has nothing to call.** **Design rules, FROZEN:** (1) strictly read-only end to end — no engine, no signals, no writes; killable any moment without system effect; the witness must never notice the observer. (2) **Displays beliefs and holdings, never recommendations — no suggested-trades surface exists or may be added.** Lifecycle states shown with actual epistemic status; observational content visibly stamped; forward-zone P&L only as the ledger's own records. Rationale on the record: a UI that recommends is a hand-fitting console with a broker account nearby — manual action on a displayed observational signal creates an untracked shadow strategy living in operator memory instead of a ledger. (3) Typed event schemas mirroring JSONL shapes as TS interfaces; every timestamp through a dual-convention type (close + derived open = close − TF) — no naked timestamp reaches the DOM; the open/close trap killed at compile time. **Panels:** clean-store price chart with per-TF marker toggles (dual-convention placement); per-TF narrative feed; phase/trend/segment; executor pulse (heartbeat age + watchdog — the is-the-witness-alive panel this weekend proved necessary); open positions + ledger tail (entry/stop/basis/stake as recorded); permanent zone banner. **Operational:** local-only, ui/ in repo, yarn/Vite documented in README ops; suite gains a backend-contract test — endpoints verified read-only, response schemas verified against actual payload shapes (TS types are only as honest as the payloads they mirror).

18. **Language strategy (dated 2026-08-15, forward-looking, no action now): Rust port registered as a post-round-1 option, perimeter-first.** The operational shell (collector/poller, store I/O, executor harness — fixed contracts, long-running reliability needs, no research churn) is the natural first candidate once its contracts stabilize; the engine ports only after round 1 crowns a validated champion, and only behind a full parity harness holding decisions to bit-identical equivalence (tolerance-aware for float features, exact for labels/spawns/gates/fills — numeric summation-order divergence is the known hazard). **The language-neutral seams (YAML strategy definitions, JSONL event streams, dotted-path config tree, feed/engine separation) are load-bearing for this option and must not be casually Pythonized.** Rationale: rewrites suit settled systems; this one is mid-metamorphosis, and the campaign's 29s / 1-poll-per-minute profile gives Rust no performance case today — the case is long-run operational rigor, and its moment is post-validation.

**Measured feed contract (2026-08-17, probe capture logs/feed_probe/2026-08-17_0756.jsonl archived as evidence; SUPERSEDES both prior theories).** (1) Served index 0 = forming bar — operator's prior-platform recollection CONFIRMED by measurement (newest-stamped 107/107 polls, heavy same-minute OHLCV accrual; hypothesis source credited). (2) Index >=1 IMMUTABLE across the capture — first appearance at index 1 is final; no post-close revision in the measured (cash-open) regime. (3) All three consumers audited: collect_feed, narrate.live, paper all slice iloc[:-1] AFTER fetch_page's ascending sort — correct under the contract; pinned by tests/test_feed_contract.py on an archived raw page. (4) Friday divergence theory redirected and PARTIALLY verified: replay side EXACT (raw 40 / bin-mean 57 = 0.70 to the decimal); live side unresolved — 0.34 implies live bin mean ~118, not matched by the first-seen-bar anchor candidate (yields 1.58); the live process's bin state was not persisted (the embed rule's reach extended: canonical warm procedure OR bin-state embedding in decision events is REQUIRED for live/replay reproducibility — registered hazard). CONFIRMED CONCRETE DEFECT regardless: live loops (narrate.live, paper) anchor tod at first-seen bar, not the canonical 21:30-London trading-day anchor — bin-key misalignment; fix queued for next restart cycle. (5) Two-poll confirmation RETAINED — capture samples Monday cash-open only; a post_close thin-tape probe window is required before any latency reclaim. 

**tod-anchor fix + canonicality extension (2026-08-17, restart tonight in the 21:00-22:10 London pause — structurally zero-bar gap).** Both live loops (paper + narrate.live) now derive bin keys from the canonical 21:30-London trading-day anchor via canonical_tod() (shared with the resampler); pinned by tests/test_canonical_tod.py incl. the Friday bar's exact bin (1127) and the anchor-crossing rollover. **Canonicality note extended: ALL live label events from go-live (2026-08-14 15:04Z) until tonight's restart carry the tod-misalignment caveat; the settled replay is canonical for the entire pre-fix period.** Monday checkboxes confirmed: first sync-while-live clean (store.py verify 28/28 both slugs); Sunday restart's persist-then-feed proof formally logged — raw and clean stores both advancing during live operation.

19. **Crash-coverage honesty + warm-scope asymmetry (2026-08-17).** (a) Paper gains UNCLEAN_PREDECESSOR detection, a per-poll checkpoint sidecar (reports/paper/checkpoint.json — append-only ledger stays clean), and predecessor-anchored coverage gaps (STOP.last_processed | checkpoint | predecessor warm_through — never this run's warm_through: a post-crash sync erases downtime; "warm==last-processed" is FALSE after crash+sync, comment corrected). Monday's overnight outage RETRO-LOGGED as a COVERAGE_GAP (Sunday unclean death -> 06:55Z, extent unrecoverable — no checkpoint existed). README aligned. (b) **narrate live/replay warm asymmetry registered as a standing reconciliation caveat:** live warms via cash_sessions (in_cash-filtered, cash-anchored bins — banner now says "last CASH bar", confirmed intentional); replay warms via trading_sessions (extended, 21:30 anchor) — comparator replays MUST match the live warm scope or attribute diffs between tod and warm-scope causes. (c) **Today's regrade (06:56Z->close, cash-scoped comparator per the caveat): 12/12 live labels match label-for-label and rel_volume to the hundredth, including both near-threshold prints (08:21 rv 1.37, 08:25 rv 1.08); archived logs/feed_probe/regrade_2026-08-17.txt beside the probe capture.** Today's live session was internally consistent under its cash anchoring; tonight's pause restart moves live to the canonical anchor.

**Register 19 amendments (2026-08-17, append-only):** (1) Monday outage magnitude CORRECTED ~7-11h -> ~2-3h of tape: sync-log fossil (+176 minute bars) against store-verified density (482 bars Sun 22:05Z->Mon 06:55Z, ~0.91 fill) bounds Sunday-process death ~=03:10-05:00Z Mon (band widens with thin overnight tape; Friday-close leftovers possible). Ledger retro-log amended in place (append-only NOTE). (2) **Feed-contract second opinion COMPLETE: independent analysis of the raw capture concurs in full** — 107/107 newest-at-index-0, 19/19 multi-poll minutes accrued, 0/23 settled-bar mutations, 15/18 handoff changes. (3) Procedure note: sync-log "+N new" counts are a recoverable death-time fossil for any unclean exit of a persisting process; obsolete once checkpoint.json is live. (4) Regrade window CONFIRMED COMPLETE: live_ladder_v4.csv contains exactly the twelve matched labels — no 08:39Z/08:47Z prints exist in the artifact (recollection contradicted by the file; compute-don't-assert, again). (5) **Regrade scope fence: 12/12 validates the 1min layer's internal consistency ONLY** — 1min rel_volume is largely invariant under a consistent anchor shift; signal/context-TF assembly under the shifted anchor is unmeasured and remains covered by the pre-fix canonicality caveat.

**Register 19 amendment — item (4) OVERTURNED (2026-08-17, append-only).** Contested and re-examined against the live file: live_ladder_v4.csv is 12 lines / 4057 bytes / mtime 12:44 local, ends 08:25Z, and **no process holds an fd on it (lsof empty)** — it is a MANUALLY SAVED EXCERPT, not the process's output artifact; the running process's labels live in terminal scrollback + the heartbeat stream. Evidence classing corrected: **the morning paste and the heartbeat stream are artifact-grade records, not memory** — heartbeat arithmetic (08:41Z "2 bars since" -> 08:39Z; 08:56Z "9 bars since" -> 08:47Z; counter validated against agreed 08:25Z/08:04Z labels; 10:39Z triangulated from two heartbeats) corroborates afternoon labels at 08:39Z, 08:47Z, 09:11Z, 10:26Z, 11:26Z. The prior "recollection contradicted by the file" entry examined an excerpt believed to be the artifact — the compute-don't-assert failure was the implementer's (verifying against a copy without checking provenance/fd), not the reviewer's. **The 12/12 regrade therefore covers the excerpt window only.** Replay comparator run on the five heartbeat-derived timestamps (label-presence level; live payloads unpersisted): results recorded in commit. Operator action before the 20:00Z restart: capture terminal scrollback — it is currently the sole record of the afternoon live stream.

20. **History rewrite + fd-1 adjudication (2026-08-17).** git filter-repo ran ~12:43Z, reset the working tree; commit-map archived logs/history_surgery/commit-map_2026-08-17 (35 entries; maps cited hashes a541aed..70a55a9 to post-rewrite identities). **70a55a9 AMENDED:** live_ladder_v4.csv was NOT a manual excerpt — the narrate process (pid 40834) holds fd 1w on an ORPHANED inode (187405622, 20,275 bytes = full stream incl. afternoon) while the directory entry names the filter-repo-restored 4,057-byte morning version; under the stdout redirect, scrollback is stderr/heartbeats only. Orphan unrecoverable on macOS without debugger; afternoon labels ACCEPTED-LOST; heartbeat stream = surviving witness (operator capturing scrollback). Ledger verified intact through the 13:11Z amendment — no rewound-file STOP risk. **Presence-checks amended: 10:26Z directly derived ("0 bars since"), replay label-less under the same cash-scoped comparator; 10:39Z (the triangulated stamp) checked: ALSO label-less (rv 0.55) — TWO open divergence candidates; the "hazard didn't bite today" line is WITHDRAWN pending resolution (unresolvable without the orphan; recorded as unresolved).** Rules adopted: persist heartbeats/stderr in the queued session-JSONL; tonight's ladder redirect to a dated untracked file; **history surgery only on a stopped system with a clean tree.**

**0e7ae9c entry note + queued analysis (2026-08-17, post-restart, no urgency).** (a) **Orphan-size cross-check:** the fd-1 orphan's 20,275 bytes ≈ ~48 labels at the morning file's bytes/label — consistent with the heartbeat-derived density: **the two surviving witnesses corroborate each other.** (b) QUEUED — heartbeat-derived presence study: derive every label stamp from the captured heartbeat stream (each "N bars since" yields one; >=14 afternoon stamps derivable, lower bound), presence-check the full set in time order under the matched cash-scoped comparator, plot match/miss against session time. **Hypothesis: bin-key pollution accumulates through the session** (warm-keyed bars dominate early -> clean morning; live-keyed bars accumulate -> progressive divergence) — predicting a RAMP, not a flat rate. Whatever the shape: archive beside the probe capture as the tod-defect's measured live signature, referenced from this entry. BLOCKED ON: the scrollback capture landing at a readable path (suggest logs/history_surgery/scrollback_2026-08-17.txt).

**tod-defect measured live signature (2026-08-17, archived logs/feed_probe/live_signature_2026-08-17.txt beside the probe capture; complete-session input logs/history_surgery/scrollback_narrate_2026-08-17.txt, old narrate died ~13:43Z, n=407 corroborates).** 24 heartbeat-derived stamps (19 afternoon; double-witnessed 10:39Z + 12:52Z), all on the cash grid, presence-checked time-ordered under the matched comparator: **21 MATCH / 3 MISS — and the RAMP HYPOTHESIS IS REFUTED.** The misses form a mid-session CLUSTER (10:26Z, 10:39Z, 11:02Z) with clean matches before AND after (07:09->10:09 all match; 11:26->13:33 all match, spanning the 12:11->13:42 decline and the 12:56Z 15min flip, incl. two NO_DEMAND prints). Divergence cause is a transient around 10:26-11:02, not accumulating bin pollution — mechanism unresolved (payloads freed with the orphan at process exit; loss accepted). **First full-session feed-latency distribution registered: n=407, min 1s / median 38s / max 63s (single-poll; within the 9-69s envelope; paper adds one confirmation cycle) — the measured latency floor, archived with the capture.**

21. **Consolidated session findings (2026-08-17).** (1) Divergence-cluster CLARIFIED: replay labels all three "miss" stamps TEST/no-core (rv 0.51/0.55/0.38) vs the 10:19Z signature — CLASSIFICATION divergence, not absence; cluster shape explained by signature-registry state, not bin pollution. (2) Spec-review observation: 10:19Z climax-vs-upthrust precedence — **scenario s9 proposed for the next ruling cycle: up-spike-retrace at range high -> UPTHRUST must win.** (3) Session record: both climaxes 1min-only phenomena; NO_DEMAND chain predated 15min trend establishment — abstention fully accounted for at those two gates; **day flagged as an H7-dossier exhibit.** (4) Macro: no red event 2026-08-17; 10:29-30Z burst logged structural-uncalendared; 13:30Z = US open. (5) **Second unclean paper death** (VS Code crash #2; pre-migration foreground process): death-minute bounded by store tail per the sync-fossil technique — FIRST REUSE of the registered procedure; operator to supply the timestamp for this entry's amendment. Paper relaunched immediately on current HEAD via README nohup procedure (pause-window preference void once the process is dead); first live UNCLEAN_PREDECESSOR expected and to be verified in the ledger; the new START's COVERAGE_GAP annotated: warm_through-fallback (06:55Z) OVERSTATES — predecessor predates checkpoint.json; true hole = store-tail -> relaunch. Paper stderr scrollback lost with the IDE (uncaptured; expected banner-only; stores verified advancing all day). Narrate-process survival across the crash = the migration fix's first live validation, whichever way it went (operator to record). (6) Tonight's pause-window step reduces to: verify both processes healthy on canonical bins, then commit — captures, ledger delta, onboarding doc.

**Register 21 amendment — cluster is a MEASUREMENT ARTIFACT (2026-08-17, append-only).** The presence study's miss criterion was asymmetric: heartbeat attestation counts ANY truthy label (TEST included); the replay comparator filtered at structural-core level. Replay prints truthy TEST labels at all three "miss" stamps (10:26Z/10:39Z/11:02Z, vs the 10:19Z registry signature); live's label KINDS at those minutes are unrecoverable (orphan), and the heartbeat evidence is equally consistent with live printing the same TESTs. **Corrected finding: 24/24 at label-presence level — the only level the surviving evidence supports; core-level comparison UNMEASURABLE, not failed. The earlier "10:26Z label-less under the identical comparator" statement is FALSIFIED by the replay output.** The pre-fix session's measured record: **no detectable live/replay divergence at any measurable level.** The warm-state/registry hazard class stands on Friday's payload-level evidence ALONE; today is struck as its second exhibit. Signature archive amended in place (append-only note). Still awaiting: operator store-tail timestamp (death-minute bound); narrate survival check across the 15:0xZ crash (nohup migration's first live validation) — to be recorded on report.

**Register 21 CLOSED (2026-08-17).** (1) Narrate survival across the 15:0xZ crash CONFIRMED — nohup migration's first live validation PASSED: pid 37662 alive post-crash with no controlling TTY (detached — exactly the installed property). Heartbeat-cadence continuity through the crash window not separately run; survival established by pid/TTY evidence alone. (2) Death-minute bound RESOLVED — fossil extracted before the sync could heal it (the race flagged in this entry was WON): store gap 15:05->15:33 open-stamps; last persisted bar opens 15:05 (close 15:06) -> predecessor death ~=15:06-15:08Z, confirming the ~=15:06 estimate to the minute; first post-relaunch bar 15:33 matches the 10-bar catch-up reach exactly. Data hole 27 min (straddles the 15:30Z cash close, consequence-free; reconcile clean); decision hole ~36 min as registered. Evening sync heals the hole; this register is then its sole witness — as intended. **From 2026-08-18, checkpoint.json retires forensic death-dating.**

22. **Session-JSONL persistence — PROMOTED to the next build slot (2026-08-17).** Today demonstrated the gap three ways: the live narrative stream died twice with its file descriptors; the heartbeat stream survived only as an accidental witness; and "where is the continuous narrative stored?" had no good answer. **Spec:** both live loops persist their full event stream (labels, phases, hypothesis events, heartbeats/stderr equivalent) to dated JSONL under logs/, append-per-event, alongside the existing console output. **Scope (reviewer recommendation, operator may override): ALL segments** — paper's perception already runs 24/7; persist what the engine actually sees, with existing observational tagging for non-cash segments, so the persisted narrative matches the continuous view rather than the cash-only console projection. **Test: a killed process's JSONL ends within one event of its checkpoint.** Narrative-only by construction — persists what narrate already emits; feeds no thresholds, rulings, or tracked signals.

**11:46Z cascade analysis (2026-08-17; archived logs/narrate/cascade_analysis_1146_2026-08-17.md; non-evidential).** (1) Ladder rungs VERIFIED as full-perception TFs (complete context pipes: PHASE_EVAL + SWING_CONFIRMED on 3M/5M) — supersedes the prior "label-only rungs" characterization; **perception is all-TF, decision is single-TF.** (2) Measured cascade: decline labels 1M/3M 11:48 -> 5M 11:50 -> 15M 12:00; top-knowledge 1M 11:50 -> 3M 11:57 -> 5M 12:05 -> 15M 12:45 — k-lag scales linearly in wall-clock; **the signal TF learned the top 58 min late, 72% of the move spent**; trend-1 onset 1M 12:02 (aged 44) vs 15M 12:45. (3) The 12:27Z rv-5.99 climax is STRUCTURALLY NULL at 3M — aggregation deletes 1M events entirely, while decline texture (rv<1) is rung-invariant. (4) Filed under the H7 dossier + lab composition candidates (rung-consensus / ladder-trend-anchored H4-mirror variants) — round-1 material only; no engine or threshold implications now. (5) Six-rung note: rungs are full-perception TFs.

23. **Store-write race — discovered at commit time, fixed same day (2026-08-18).** (a) FINDING: `store.py build_one` truncate-rewrote every clean-store CSV in place (`df.to_csv` directly onto the target path), so any concurrent reader saw a truncated or empty file mid-build. Two real exposure classes: the git-snapshot race (OBSERVED instance: commit `9bcf9ec` captured `clean_finsa/uk100fut_1h.csv` as a 0-byte blob mid-rewrite; on-disk content was bit-identical to pre-commit within ~2 min; restored in `c3e806b`) and the graver live-reader race — a live loop warming from store during a rebuild reads truncated history, and warm state determines bin contents (register hazard rule; warm-anomaly cost is established precedent). (b) FIX: atomic writes in the store builder — `_atomic_to_csv` / `_atomic_json_dump` (temp file in the same directory + `os.replace`), covering all clean-store CSVs and `_report.json`. Pinned by `tests/test_store_atomic.py`: a crash at any point mid-write leaves the old file complete and unchanged; a reader opening at any moment sees old-or-new, never empty; temp files proven same-directory (same-filesystem atomicity). `*.tmp` gitignored against crash leftovers. (c) POLICY RECONCILIATION (stated decision): the README's 2026-08-14 "Excluded: raw/clean data stores and logs" was NEVER ENACTED — `.gitignore` exclusions sat commented out and stores/logs have been committed throughout. **README amended to match the enacted practice; the practice wins** because the ~30-day retention floor makes committed stores the only durable history — an untracked store loses everything beyond retention on the first mishap. The gitignore comment block now states tracking is deliberate. (d) HISTORY-SURGERY RULE ADDENDUM (extends register 20's rule): "stopped system + clean tree" now provably includes **no store builder running** — paper's per-poll persist path calls `build_one`, so a live paper process IS a store writer; kill order: processes first, then verify no build in flight (no `*.tmp` in clean_finsa/, stable mtimes), then surgery. Atomic writes narrow this race to the rename instant but the rule stands — surgery on a moving tree is the class, not the instance.

**Finding 23 annotation — the 2026-08-17 12:43Z rewrite adjudicated (2026-08-18; the "what did the surgery remove" question raised that day, answered now).** METHOD: pre-rewrite objects are unrecoverable (gc'd locally — old SHAs from the commit-map resolve to nothing; GitHub holds only post-rewrite objects — fetch-by-SHA refused "not our ref"), so the question was adjudicated from surviving evidence, not the filter's arguments — `.git/filter-repo/` archives maps only (commit-map = the archived logs/history_surgery copy, ref-map, first-changed-commits), not the invocation. **BRANCH (b) — STORE BLOBS WERE NOT STRIPPED, by direct observation:** pre-rewrite-dated commits still carry full store diffs (`4ad5468` "Refresh data" 2026-08-11 diffs clean_finsa/ files; `data/` and `clean_finsa/` blob history reaches back to `b0829e0` 2026-08-11); the untrack commit `12ded63`'s own message — "Past history retains earlier store snapshots" — remains TRUE post-rewrite. The committed-stores-durable-history doctrine was never violated. **What the rewrite did (INDICATED, NOT PROVEN — operator confirmation invited):** identity scrub. Evidence: the ROOT commit itself was rewritten (first-changed-commits `c85d0689`→`9dea94e6`) with every one of the 35 then-existing commits changed and no tree-level removal observable; surviving metadata shows exactly ONE author+committer identity across all history (the GitHub noreply address), zero traces of any other name/email in commit metadata; and the rewrite immediately preceded the repo's first push (remote advertises only post-rewrite objects; gitignore update + store re-add landed 6 min after the rewrite). A small path removal alongside cannot be excluded — absence is unobservable without the old objects — but no evidence points to one. **COROLLARY, added to the history-surgery rule (register 20, extended by finding 23c): store history is never filtered, ever** — adopted prophylactically per branch (b); the stores' git history is the only durable archive beyond the retention floor, and no future surgery objective (identity, secrets, size) may touch store paths. **COST OF DELIBERATE STORE TRACKING (priced, so tracking stays a decision, not a drift):** tracked stores+logs = 241.3 MB on disk (all tracked files 244.9 MB); measured append ≈ 3.3 MB/day raw (calendar-day basis across all store CSVs; ~100 MB/month, ~1.2 GB/yr raw). Git-side cost is lower — the full-history pack is 52 MB (~4.7:1 vs content; CSV appends delta-compress well; ~150 MB loose objects pending gc from the latest data commits). **Any future size pressure gets solved by anything EXCEPT filtering store history** — acceptable: partial/sparse clones for consumers, coarser data-commit cadence, archiving this repo intact and continuing in a successor — the filter is not on the menu.

24. **Watchdog calendar boundary — the pre-registered class from the Sunday runbook, now observed (2026-08-18).** (a) INCIDENT: 11 false WATCHDOG_STALL events 2026-08-17 21:11:33-22:03:01Z. `expect_prints` encoded the daily feed pause in LONDON hours (21:00-22:10 London); the provider pauses on UTC (DATA.md's ~22:00->~21:00 UTC day). One hour of BST offset = one hour of false alarms. MEASURED from tonight's store: last pre-pause bar opens 20:59Z (tape live to 21:00Z exactly), first post-pause bar 22:05Z; Sunday reopen 22:05Z corroborates. (b) FIX (`522a5cb`): calendar re-anchored to provider UTC — quiet window [21:00, 22:10) UTC (measured + 5 min reopen margin), weekend quiet Fri 21:00Z -> Sun 22:10Z (previously London-dayofweek). Pin test = the incident (tests/test_watchdog_calendar.py): the first and last false-event timestamps verbatim must be suppressed under the fixed calendar while a synthetic 5-quiet-poll stall at Monday 14:00Z still fires; measured boundaries and GMT-month behavior pinned. A STALE PIN CORRECTED (`b1acfe7`): tests/test_paper_watchdog.py asserted 20:30Z Friday is pause — it pinned the defect itself; store-measured tape prints to 20:59Z. (c) LEDGER: append-only NOTE added marking the 11 events as calendar-offset false positives, calendar fixed at 522a5cb. No restart urgency — false stalls are ledger noise, not risk; the fix rides the next natural restart, and until then the running process may print false stalls in the London-offset window. (d) FAMILY SWEEP (London-hours-where-provider-is-UTC): **store.py US/gold sessions — the DATA.md known sibling — ALREADY FIXED in code** (SESSION_TZ = America/New_York per instrument; comment records the old London hardcode was off by one hour in DST-divergence windows); narrate.py `in_cash` and loop.py EOD/embargo are EXCHANGE-anchored London — CORRECT for LSE instruments (the exchange, unlike the feed, genuinely lives on London wall time); sync/collect carry no time-window logic. NOTED, not a defect: `canonical_tod`'s 21:30-London trading-day anchor is an owner-ratified ENGINE convention shared by resampler and both live loops (consistency is the requirement, and it holds); seasonal nuance recorded — in BST months the anchor (20:30Z) sits in live pre-pause tape so the final ~30 pre-pause minutes bin into the next trading day, in GMT months it (21:30Z) sits inside the provider pause; session composition near the boundary therefore differs by season. Watched, no action. (e) SEASONAL DIMENSION, stated for the record: **the bug class is invisible in GMT months** — London == UTC and the false window closes on its own. A November observation that the stalls "stopped happening" would be the offset closing, NOT a fix. Any London-anchored provider-facing logic must be tested in BOTH offsets (the new pin does); this entry is the reference when a DST transition next changes live behavior unexplained.

25. **STANDING RULE — Evidence Regression (adopted 2026-08-18; run ad hoc three times this week, now mandatory).** Every defect finding carries a BLAST-RADIUS field before closure: (i) touched code paths enumerated; (ii) every evidence artifact class classified UNTOUCHED / TAINTED-RERUNNABLE / TAINTED-UNRECOVERABLE — **and test assertions count as evidence artifacts:** the sweep includes grepping existing tests for pins derived from the defective belief (motivating instance: tests/test_paper_watchdog.py's 20:30Z assertion ENCODED finding 24's bug — the suite was green because it agreed with the defect); (iii) rerunnable artifacts get rerun-and-diff on fixed code, diff archived, confirmations logged as evidence; (iv) unrecoverable spans get honestly-scoped caveats per the canonicality precedent (tod-defect pre-fix window); (v) any downstream decision fed by an amended result is explicitly re-adjudicated — kills, GOs, prioritizations — by walking the trial-log chain; (vi) lockbox discipline is never suspended for a rerun. COROLLARIES: **(a) engine commit hash mandatory on all lab/census artifacts.** Retroactive verification (2026-08-18): T3 records its engine state (boundary_check.head 10aabb60); T1/T1b/T1c/T1d artifacts record NONE — gap annotated in the trial log; mitigation: each census was committed atomically with the code state that produced it, so the engine state is recoverable as the introducing commit (T1=12e1d75, T1b=74a0536, T1c=0376744, T1d=1c02853); recoverable-by-parentage is acceptable for the four legacy artifacts, in-artifact hash is mandatory from now on. **(b) Pin provenance mandatory** — every incident-pin test states its evidence source (measured artifact vs spec-derived); a pin whose provenance is a belief is the finding-24 failure mode. **(c) Test invocations must propagate exit codes** — no tail/pipe masking; pipefail or direct invocation (motivating instance: 522a5cb committed on an unseen red — `pytest | tail` returned tail's exit status).

**Finding 24 amendment — Evidence Regression applied, first formal instance (2026-08-18).** BLAST RADIUS: touched code = `expect_prints` alone, consumed ONLY by the live stall alarm (paper.py watchdog guard); it feeds no bars, bins, labels, features, or replay paths. EVIDENCE CLASSES: all lab-cycle artifacts (T1/T1b/T1c/T1d/T3), event studies, campaign reports, regrade/signature captures — UNTOUCHED (no data path through the defect); ledger — TAINTED-RERUNNABLE at the annotation level: 11 false WATCHDOG_STALL events, cured by the append-only NOTE (the events stand as honest history, reclassified); test suite — one TAINTED pin (test_paper_watchdog.py 20:30Z), cured `b1acfe7`. DOWNSTREAM DECISIONS: none — no kill, GO, or prioritization consumed a watchdog event; trial-log chain walked, no re-adjudication required. THE WEEK'S THREE INFORMAL PRECEDENTS this rule formalizes: (1) tod-defect canonicality caveat (all pre-fix live label events caveated, settled replay canonical); (2) comparator TEST-blindness re-cut (presence study re-run symmetric, 24/24, "10:26Z label-less" falsified); (3) T1d pooled->matched re-cut (census defect, matched-only cell recomputed, gate re-adjudicated and HELD). DEPLOYMENT: the running paper process still carries the London calendar; **the calendar fix deploys at the next natural restart — item 22's session-JSONL deployment restart is the designated vehicle; until then the nightly ~21:10-22:03Z false stalls (BST offset window) are EXPECTED and covered by the ledger NOTE** — do not re-investigate them. DATA.md's US/gold "fix before NASDAQ stage" note amended to fixed-at-commit (`6115e8a`, SESSION_TZ) per the finding-24 family sweep.

26. **Standing travel model — daily machine suspension (2026-08-18).** (a) NEW STANDING FACT (operator-supplied): the laptop TRAVELS DAILY (commute). `caffeinate -is` does not survive clamshell-on-battery — machine sleep freezes both live loops silently. Daily suspension holes are EXPECTED from now on, not incidents. (b) OBSERVED INSTANCE (this morning): 07:17:11Z EXECUTOR_ERROR (URLError, DNS gone) = network teardown at sleep onset; both processes froze and self-healed on wake; no crash, no stall, watchdog silent (correctly — polls stop during sleep, quiet-poll counter never accrues). Span bounded by store fossil before sync healed it (THIRD field use of the sync-fossil technique, after the register-19 and register-21 death bounds): last persisted bar opens 07:02Z, first post-wake 07:40Z — 38 min tape hole; ~18 cash bars of decisions missed outright per narrate counter arithmetic (wake catch-up recovers the fetch-window reach late; the remainder is the hole). Ledger NOTE appended. (c) DETECTION BUILT (promoted out of item 22's riders as an immediate small order, commit `25d9845`): per-poll wall-clock jump check in BOTH live loops — elapsed since last poll > 5 min threshold -> paper logs ONE SUSPENSION_GAP ledger event {from, to, span} before processing resumes; narrate prints the heartbeat-stream equivalent. Pin test = today's incident shape (tests/test_suspension_gap.py, store-measured provenance per rule 25b): a frozen span produces exactly one gap event with correct bounds, not an error stream; normal cadence and exact-threshold produce none. Deploys at the next natural restart (item 22's restart remains the vehicle; item 22 remains the active build slot — this order was its rider, promoted, not a queue jump). (d) EXPECTED BASE-RATE IMPACT: benign at frozen_v1's trade rate (0 trades to date; commute windows are early-session UK time; EOD-flat means no overnight position can be stranded) — any future strategy with holdings through 07:00-08:00Z must revisit this entry. (e) OPERATOR ALTERNATIVE, noted for the record as an OPEN OPERATOR CHOICE, not an order: the Mac stays home on AC power = zero suspension holes; until/unless chosen, one SUSPENSION_GAP per commute day is the expected ledger texture and requires no investigation.

**Finding 26 amendment — travel model corrected (2026-08-18, append-only; operator testimony).** The commute is INTERMITTENT, not daily — the operator travels occasionally with the Mac; frequency is operator-variable. Migration to a dedicated always-on machine is PLANNED-FUTURE, not hypothetical. CONSEQUENCE, superseding entry 26(e)'s framing: SUSPENSION_GAP events on TRAVEL days are expected texture; on NON-TRAVEL days they are SIGNAL (power-settings drift, unexpected sleep) and warrant a look — the superseded daily framing would have normalized exactly those. The do-not-investigate stance applies PER-EVENT and only when the operator can attribute the day to travel; an unattributed SUSPENSION_GAP is a finding until attributed.

**Register 25 amendment (2026-08-18, append-only).** The evidence-artifact taxonomy explicitly includes BELIEF-DERIVED PROSE IN CODE — comments and docstrings encoding factual claims — for blast-radius sweeps. Precedents: the 20:30Z test pin (finding 24) and the suspension_check "daily" docstring (finding 26 rider). A wrong comment fails no test and diffs against nothing; it propagates by being read.

27. **Campaign Part C — forward-zone observational studies (CONSIDERATION registered 2026-08-18; not an order, not built).** Once the forward-migration readout exists (built, `b88da38`), evaluate promoting it to a standing campaign Part C: forward-zone observational studies run weekly alongside FORWARD_PAPER — because the working-set studies are correctly frozen pins (bit-identical by design) and the forward zone is where evidence actually accrues. The reviewer's "dossiers deepen weekly" description was belief-derived prose (register 25 taxonomy) until something like Part C exists: today, a weekly campaign rerun changes only the FORWARD_PAPER section. Scope if adopted: the forward-migration readout + any future forward observational studies, all stamped exhibits-never-validation, zone-fenced, engine-hash-embedded; the evidential working-set pipeline stays untouched. Decision deferred to the operator at the weekend review, with the first forward readout in hand.

28. **RULING (operator-approved 2026-08-18, reviewer-recommended APPROVE): rel_volume emitted on structural-only LABEL events** — additive/observational, already computed for qualification. VERIFICATION BURDEN, satisfied before implementation: the sole post-emission consumer of a label's rv is migration.py:38 (observational study); the decision layer (hypotheses/gating/router/broker) never reads narrative events at all — decisions consume feats objects directly. No separate finding. Implemented at pipeline.py (commit noted in log). **EVIDENCE REGRESSION on the recruitment defect (rule 25, second formal application):** July migration artifact classified TAINTED-RERUNNABLE -> full campaign rerun under the fixed instrument; diff archived reports/evidence_regression/migration_rerun_2026-08-18.json. Result: rv coverage 257/510 -> 510/510 (conflation cured); 4/510 events flipped to recruited (previously-unmeasured parents with rv >= 1.2); depth distribution unchanged; group means shift at noise level (largest at n<=29, single-digit bps). H9 DOSSIER RE-READ: no reading depended on the biased split — H9's recruited-chains clause remains untested-at-power, now on unbiased data. FORWARD READOUT RERUN + 11:46Z RE-GRADE: recruitment half vacuous -> GRADABLE -> **MATCHED on measured data** (both window events' parent rv 0.53/0.42 < 1.2 — predominantly-unrecruited confirmed; the predicted 0.69-0.85 range was eyeballed from the 15M parent, which never formed a migration event — the measured parents are 1min->3min; direction of the prediction right, the precise range not). DEPTH HALF STANDS AS MISS with the semantics finding (label-arrival cascade != persistence chain).

29. **STANDING LESSON — pre-registered expectations are computed objects (2026-08-18).** Where the clauses and the data both exist at registration time, apply one to the other MECHANICALLY before registering; a prediction that could have been computed and wasn't is an assertion wearing a pre-registration's clothes. Assertion words in a pre-registration — "trivially", "obviously", "clearly" — are REVIEW FLAGS. Motivating instance: the 11:46Z depth expectation was written with both the migration clauses and the replay narrative in hand; running the instrument over the replay (as the graded readout later did) would have returned depth 1 immediately and surfaced the cascade-vs-chain vocabulary split BEFORE registration instead of after a graded miss.

**Register 27 amendment (2026-08-18, append-only).** Reviewer recommends ADOPT at the weekend review: the forward readout validated the channel and caught a standing-instrument defect (register 28) on its first run. Recorded alongside, for the Part C design: (a) the VOCABULARY SPLIT from the graded miss — cascade (label arrival at successive rungs) and migration chain (persistence, >=2 children per parent window) are different objects; Part C dossier pages must name which they report. (b) The aggregation-decay census (trial log, gated behind T3b) is the instrument that the 11:46Z prediction actually described — per-event rv survival across rungs — and the ruling in register 28 (rv on structural labels) is a prerequisite it now has.

30. **Standing sealed-window schedule (adopted 2026-08-18; docs/lockbox_policy.md is the reference).** The first two weeks of each quarter are BORN SEALED, anchored 2026-09-01 (months Sep/Dec/Mar/Jun, days 1-14 inclusive, UTC). Windows are declared FORWARD ONLY, chosen by calendar never by content. Enforcement in code: `is_sealed`/`refuse_if_sealed` (store_loader), refusal test + truth table pinned (tests/test_sealed_windows.py); all forward readers skip sealed spans automatically and count skips in artifacts (forward_migration first, mandatory for Part C/scoreboards/censuses). Live processes stream through sealed windows as live consumers — the seal governs reads-for-research, not perception. Schedule changes are registered rulings, forward-only. The Aug 4-14 legacy lockbox is separate, unchanged, and FIRST TO BE SPENT.

31. **Hypothesis scoreboard — World A production build, SPEC (2026-08-18; build on approval; touches the live loop so it RIDES A RESTART — bundled with item 22's deployment restart).** MECHANISM: every hypothesis reaching CONFIRMATION is tracked as a SHADOW TRADE under paper's fill assumptions REGARDLESS of gate outcome, tagged with the stopping gate (which gate blocked it, or ACTED for the real path). Shadow entry at the same next-bar-open/refinement semantics the real path uses; shadow exits under the frozen exit scheme. OUTPUT: per-hypothesis table, weekly (campaign/report section) + on-demand (CLI): n confirmations, shadow trades, points won / points lost, biggest win, biggest loss, win rate, avg R — COUNTS ALWAYS ADJACENT TO POINTS; header states fills are SIMULATED/IDEALIZED. FENCES: shadow participants run behind the candidate guard — perceive + report only; a separate shadow ledger file (reports/paper/shadow_ledger.jsonl); the real ledger and frozen_v1 behavior are untouched (invariance test: engine decisions bit-identical with shadow tracking on/off). NEW HYPOTHESES ENTER SHADOW-LIVE AT REGISTRATION — which requires a candidate-guard amendment as part of this approval: H6-H9+ identifiers become permitted in ONE dedicated shadow module only, and the guard test tightens to enforce that no engine decision path imports from it (the guard's purpose — candidates cannot influence decisions — is preserved by import-direction enforcement rather than identifier ban). VERDICT-POWER: none (docs/hypothesis_lifecycle.md stage 4) — texture and prioritization input, never promotion evidence. STATUS: SPEC ONLY; build starts on operator approval; deploys with the item-22 restart.

32. **Canonical parameter registry (standing order, adopted 2026-08-18).** docs/parameter_registry.md is CANONICAL for every yardstick/threshold/default/assumption in force — value, source file:line, date set (git blame), authority. GENERATED by scripts/param_registry.py; authority comes only from the script's curated map — **a swept value with no trail is FLAGGED, never given an invented authority**. Regeneration rides the weekly campaign; tests/test_param_registry.py pins regeneration clean against HEAD (parameter changes without a regenerated registry fail the suite). README carries the rule + pointer and duplicates no values (drift precedents cited: stores-excluded, US/gold note). Pre-registrations gain a MANDATORY yardsticks field citing registry entries — enforced by test from 2026-08-19 (the trial log refuses prereg entries without it). **FIRST-SWEEP BATCH R1, for operator review in one sitting (ratify / source / fix per line):** (1) expansion.py:HORIZONS = (5, 15, 30) — expansion-study windows, no cited ruling; (2) expansion.py:NOMINAL_BUFFER_PTS = 0.5 — expansion breakout buffer, no cited ruling; (3) macro.py:NFP_ANCHORS = {(12,30),(13,30)} — NFP release-time DST anchors, no cited ruling. Everything else swept carries a trail (founding config / frozen_v1 pin / register rulings / measured constants / operator ratifications) or is classified definitional/plumbing by mechanical rule (numeric-content test).

**Register 31 REVISION (2026-08-18, operator simplification — supersedes the trade-shadow framing; per-item disposition below).** Hypotheses are SIGNAL hypotheses: the scoreboard measures MOVE-DETECTION, not trading. (1) FIRST REGISTRY ENTRIES, operator-ratified now (in docs/parameter_registry.md, registry-first): **qualifying move = >= 1.5 x 15M ATR, one-directional, within 60 minutes, drift-adjusted; major move = >= 3 x, same clause** — changed only by re-registration. (2) APPROVED AS STRUCTURAL from the prior spec: the invariance test, ADAPTED — engine decisions AND narration bit-identical with signal-tracking on/off; and the candidate-guard amendment — H6+ FIRING CONDITIONS permitted in one dedicated signal module, guard tightened to import-direction enforcement (no decision path imports from it). (3) DEFERRED, NOT DELETED: all trade-layer machinery (shadow ledger, stopping-gate tags, fill assumptions) — may return for signals that earn a recipe. (4) BUILD TARGET: per hypothesis, computed IDENTICALLY over backtest replay and forward feed (source column states which), sealed windows auto-skipped per the c051da2 pattern: n fires; PRECISION (% of fires followed by a qualifying move in the predicted direction); COVERAGE (% of qualifying moves preceded by a fire); EARLINESS (median points-of-move remaining at fire; median minutes before the 15M trend flip where applicable); best call and worst false alarm as DATED EXHIBITS; counts adjacent to every number; every yardstick cited by registry reference. (5) T1 cited as the prototype (lead/lag census = coverage+earliness measured once, by hand); signal rows begin the day a firing condition is defined; weekly output rides Part C; build rides the item-22 restart bundle. STATUS: spec revision registered; build starts on operator approval of THIS revision.

33. **Batch R1 dispositions + horizon standardization + a determinism defect found by the regression itself (2026-08-18).** (a) DISPOSITIONS: standard outcome horizons = (5, 10, 20, 30) bars — ONE registry entry (backtest/horizons.py:STANDARD_HORIZONS, authority operator ratification 2026-08-18), cited by the event study, expansion study, migration study, the signal scoreboard, and every future outcome measurement; different marks require re-registration with stated reason; supersedes eventstudy (5,10,20) and expansion (5,15,30). NOMINAL_BUFFER_PTS = 0.5 ratified as-is (registry note: revisit if level-touch analysis becomes load-bearing). NFP_ANCHORS VERIFIED against the BLS schedule 2026-08-18 (Employment Situation, 8:30 a.m. ET first Friday -> 12:30Z EDT / 13:30Z EST): the {(12,30),(13,30)} pair is correct — authority "external fact, verified 2026-08-18"; no discrepancy. Batch R1 now empty (registry flags 0). (b) HORIZON MIGRATION under Evidence Regression (artifact reports/evidence_regression/horizons_migration_2026-08-18.json): event study and migration study EXTENDED (+30) — shared columns bit-identical (the identity IS the regression check), 30-bar column is new data with no prior reading; expansion TAINTED-RERUNNABLE -> rerun at standard marks — per-event study columns bit-identical at shared 5/30 marks; conclusion walk CONFIRMED (not assumed): H8's adverse reading rests on the identical columns, the dropped 15-bar column fed no recorded conclusion; no conclusion shifts anywhere. Forward readout migrates at its next scheduled run. (c) DETERMINISM DEFECT, found and fixed: the shared-column identity check exposed pre-existing nondeterminism in expansion's same-segment baseline sampling — set-iteration order fed a shared seeded RNG, so the 3000-bar sample differed per process (twin-run proof: cash_5 mean_range 10.79 vs 10.63). Violates the determinism doctrine (perturbation-test/bit-identical pins). Fixed: sorted segments, per-segment crc32-seeded RNG; twin runs now bit-identical. Only baseline values were affected, in the second decimal — never the per-event columns; no reading shifted. The campaign pin never covered expansion baselines — noted as pin-coverage gap, closed by the twin-run property now being deterministic.

**Register 31 addendum — scoreboard BUILT (2026-08-18; per the approved revision).** Components landed: engine/signal_watch.py (the ONE permitted home for candidate firing conditions; registry empty — signal rows begin the day a condition is defined); candidate guard AMENDED as approved (identifier ban everywhere except the signal module + import-direction test: only backtest/scoreboard.py and the paper/narrate attach points may import it — the guard immediately caught and forced a reword of the scoreboard's own docstring, working as designed); invariance PINNED (tests/test_signal_watch_invariance.py: narration AND decisions bit-identical with tracking off / attached-idle / attached-and-actively-firing); backtest/scoreboard.py (operationalization pre-registered with the trial log's FIRST mandatory yardsticks field: prereg_scoreboard_operationalization). FIRST READOUT (reports/scoreboard/signal_scoreboard.json, engine hash embedded): backtest window — H2 3 fires (precision 0/3), H4 1 fire (0/1), H1/H3/H5 0 fires, 180 qualifying episodes (62 major); forward window — H2 2 fires (0/2), 23 episodes (10 major). Zero-fire specs shown as rows (counts adjacent means showing the zero). Reading: the frozen system confirms ~once per 2-3 sessions and none of the 6 confirmations preceded a qualifying move — small-n texture consistent with the sample-starvation verdict, verdict-power none. Weekly output rides Part C on adoption (CLI on-demand now); LIVE-loop attachment deploys with the item-22 restart bundle.

34. **PURE-SIGNALS DOCTRINE (operator-ratified 2026-08-18) + signal registry v1.** A hypothesis is a FIRING CONDITION, nothing else — no confirmation windows, gates, entries, stops, targets, or management. Its only graded property: does a qualifying move follow, how reliably, how early. Trade logic (recipes) is a separate later layer, built and registered only for signals that earn one. Applied: six signal rows live in engine/signal_watch.py (prereg_signal_rows_v1, registry-cited): S-CLIMAX, S-UPTHRUST-SPRING, S-TEST, S-ND-NS, S-EFFORTLESS-SEQ (2 consecutive), S-T3B-ESTABLISHED (T1d's conditions exactly). The old 0/6 readout is RELABELED founding_recipes_at_confirmation — it graded pattern+wrapper stacks; historically interesting, kept distinct, never a signal row. frozen_v1 in paper untouched (baseline record, not a signal). FIRST DOCTRINE READOUT (reports/scoreboard/signal_scoreboard.json; base rate printed beside precision — per-direction chance ~22-23%): backtest — S-ND-NS 2046 fires, precision 24.2%, coverage 49.4% of 180 episodes, median 23.3 pts remaining; S-UPTHRUST-SPRING 347 fires, 24.8%; S-T3B 293 fires, 18.8% (AT/BELOW chance — first standing evidence on next cycle's question); S-TEST 66 fires, 21.2%; S-CLIMAX 27.3% (44 fires); S-EFFORTLESS-SEQ 17.9% (196 fires). Forward (4 sessions, small-n): S-UPTHRUST-SPRING 39.2% (51), S-CLIMAX 33.3% (12) above chance; S-TEST 3.6% (28) and S-T3B 7.1% (70) BELOW chance in-window. Texture, verdict-power none; weekly via Part C; live attachment rides item-22 restart. EPISODE-COUNTING YARDSTICK registered per the operator ruling: overlapping qualifying windows merge into MAXIMAL episodes — the shipped artifact already counted merged (code-verified, build_moves merge loop); no re-emit needed.

**Register 33 addendum (2026-08-18, per operator confirmation request).** EXACTLY which expansion columns were nondeterministic: `baseline_same_segment.{cash, overnight_asia, post_close}_{5,10,20,30}.mean_range` — the three segments with >3000 bars, where the sampler drew; pre_open (n=960, full population, no draw) was NEVER affected, and the per-event columns (by_segment_and_location) were never touched. NO recorded conclusion consumed the affected columns: H8's adverse reading rests on per-event columns; the matched-vol baseline is a separate computation; trial-log chain walked. PATTERN NOTE, promoted: three consecutive rerun-and-diffs each caught a latent defect — T1d's pooled->matched re-cut caught the census matching defect; the migration rerun under the fixed instrument sized the rv-measurement conflation (257/510 unmeasured); the horizons diff caught the baseline nondeterminism. **The diff is a DETECTOR, not bookkeeping** — rerun-and-diff earns its cost even when "nothing should change"; that is when it catches the most.

**Register 34 correction (2026-08-18, append-only; implementer error, the reports-are-generated failure mode).** Two backtest numbers in entry 34 were written WITHOUT reading the artifact (the console output was truncated; the gap was filled from memory of nothing — fabrication, caught on self-check minutes later). WRONG: "S-CLIMAX 27.3% (44 fires); S-EFFORTLESS-SEQ 17.9% (196 fires)". ARTIFACT (reports/scoreboard/signal_scoreboard.json, authoritative): **S-CLIMAX 23.2% (16/69); S-EFFORTLESS-SEQ 30.4% (34/112)** — and the correction is material: S-EFFORTLESS-SEQ backtest precision is ABOVE the ~22-23% per-direction chance rate, not below; alongside its forward 13.3% (2/15) the honest reading is "above chance in backtest, small-n contradicted forward — watch it." Every other number in entry 34 re-checked against the artifact: correct. Lesson already on the books (register ruling 3: reports are generated, not written) — this entry is its enforcement record, not a new rule.

35. **Canonical hypothesis register (2026-08-18; supersedes the un-sent scoreboard-review prompts).** docs/hypothesis_register.md is THE single authoritative hypothesis list: H1-H10, five fields each (ID / Claim / Firing condition / Origin / Status), in the onboarding reading order; trial log, scoreboard, and lab cite H-numbers from it; NOTHING runs that isn't in it — the scoreboard refuses a row whose ID isn't S-H<n> with status signal-live (validate_rows + tests/test_hypothesis_register.py). CONSOLIDATION RESULTS: (a) H10 minted from S-T3B ("in an established 1M trend, trend-direction ND/NS prints signal continuation"; data-born via T1d; H4 variant re-anchored to 1M; T3b lab serial survives as cross-reference). (b) Orphan sweep, no third category — RETIRED: S-TEST (TEST is the founding CONFIRMATION pattern = recipe layer; bare readout at/below chance), S-ND-NS-bare (unconditioned fragment of H4/H10, measured at chance n=2046; its 49%-coverage texture recorded in the register for the recall question), S-EFFORTLESS-SEQ-continuation (MIS-MAPPED — it graded the OPPOSITE of H7's registered claim; readout preserved as H7's standing adverse evidence). DISTILLED/ADDED: S-H3 (absorption cluster at level, founding params cited), S-H4 (qualified ND/NS — the classifier's qualification IS the founding context), S-H7 re-pointed to the CLAIM direction (reversal; bare variant, extreme-proximity free parameter NOT applied — audit-flagged). (c) AUDIT of all ten: H1-H4, H7, H10 PASS (recipes stripped to the register's historical-spec section); H5 disabled with AUDIT FLAG — no recorded rationale for the founding disable (a finding); H6/H8/H9 definition-pending with missing pieces NAMED (H6: free-parameter thresholds; H8: grading-model mismatch — direction-agnostic claim vs directional scoreboard; H9: chain-depth + fire-timestamp convention, otherwise mechanical). (d) Naming: one scheme, S-H1..S-H10; enforcement test includes the S-T3B legacy-serial refusal verbatim. DELIVERABLES for operator review (queue decisions wait on it): the register (audit flags visible) + reports/scoreboard/hypothesis_performance.md (every H x backtest+forward x standard metrics, gaps visible). Queue note: the T3b RECIPE-layer build (next lab cycle's first candidate) is unchanged by this consolidation; any H10 downgrade decision belongs to the operator review.

36. **Review governance + grading modes + H9 activation (operator order 2026-08-19).** (a) GOVERNANCE: reviews emit RECOMMENDATIONS, never actions — per-entry "Latest review" labels (keep-watching / deprioritize / retire / promote-candidate) against fixed criteria; register STATUS changes only via explicit dated operator decision entries; unactioned recommendations persist visibly (performance table shows label and status side by side); RETIRE requires a stated reason (it stops free evidence accrual); PROMOTE-CANDIDATE is the only gate-opening label and grants walk-forward ELIGIBILITY only — round entry is a separate decision. CRITERIA (registry-seeded; numbers implementer-proposed, operator ratification pending): promote-candidate = above per-direction chance in BOTH windows at n>=30 fires per window; deprioritize = at/below chance at n>=100 in the larger window with no redeeming metric; keep-watching = default; retire = reasoned removal only. REVIEW R1 (2026-08-19) labels applied as recommendations: H2 promote-candidate (criterion met mechanically; backtest margin thin), H10 deprioritize (18.8%/7.9% below chance, n=293/76), all other live rows keep-watching, H5/H6 pending-on-operator. (b) H5 wording per operator: "disable rationale lost to pre-crash era; preserved conservatively; revival = fresh review as new." (c) DIRECTION-AGNOSTIC GRADING MODE registered: precision-either-direction vs the either-direction base rate as its own chance baseline. H8 DEFINED under it and now signal-live (S-H8; fires on UPTHRUST/SPRING structural prints — same bars as S-H2, different claim and grading). H7 graded in BOTH modes; the movement-not-direction finding recorded in its entry. First either-direction readouts: H8 46.7% vs 44.3% base (backtest) and 50.0% vs 48.4% (forward) — AT base rate, consistent with the standing "evidence adverse"; H7-either 53.6% vs 44.3% — the volatility-localization read confirmed. (d) H9 ACTIVATED under pre-registered parameters (fire = chain depth >= 2, stamped at completing event's close; changeable only by re-registration; event-derived row): first readout 27.3% (15/55) backtest — above chance — and 8.3% (1/12) forward. (e) Guard note: hypothesis-identifier declarations (event-derived/agnostic/dual-graded row tables, H9 depth) live in the signal module per the guard's one-home rule — the guard caught and forced this placement twice during the build.

37. **Table fixes + session segmentation (operator order 2026-08-19). NO STATUS DECISIONS THIS CYCLE** — all review labels remain open recommendations; no promotions, deprioritizations, or ratifications actioned; register statuses unchanged; the two criteria numbers stay awaiting-ratification. (a) H2/H8 VERIFIED as one firing condition: identical fire counts both windows (347/347, 70/70; the differing forward best-call is a grading artifact — agnostic coverage matches different episodes). Made STRUCTURAL: S-H8's fires are now copied from S-H2's (DERIVED_FIRES in the signal module), never independently computed; both register entries carry the never-double-count statement. (b) Minutes-before-15M-flip column RETIRED — it measured time-to-next-unrelated-flip (medians 319-1810 min), not earliness; median points-remaining is the earliness metric; any replacement column requires re-registration. The flip plumbing (debug PHASE_EVAL replay dependency) removed with it. (c) SESSION YARDSTICK registered (backtest/sessions.py; registry entry session_partition; DST-proof native-tz boundaries; truth-table test in BOTH DST regimes): london/overlap/ny_only/dead/asia. TWO INTERPRETATION FLAGS recorded in the registry, operator-correctable by re-registration: London-only end = NY open per the Overlap clause (the order's "(08:00-13:30 local)" matches neither timezone arithmetic); Asia open = Tokyo cash 09:00 JST (unspecified). Label-bearing sessions (london+overlap) registered as PROPOSAL ONLY — criteria compute on whole windows until ratified. Finer cuts only by re-registration with a stated question. Episode detection, perception, persistence remain 24/5 — sessions slice reporting only. OPEN QUESTION registered: the Asia concentration of best-calls (03:40-04:04Z cluster) — regime edge vs thin-tape artifact vs unverified feed regime; the post-close thin-tape probe is the pending instrument. (d) UNION COVERAGE row added per window and per session (episodes preceded by ANY signal-live fire vs none): first readout — whole-window union 52.2% backtest. (e) Table re-emitted session-split with reports/scoreboard/READING_GUIDE.md alongside (columns, session character, base-rate logic, small-n caveats); labels marked "recommendations, none actioned, review pending operator familiarity."

38. **Scoreboard reformat + payoff measurement (operator order 2026-08-19; presentation rebuild, pipeline unchanged except two additions).** (a) TWO HUMAN VIEWS: page-1 summary matrix (live hypotheses x four contexts — backtest/forward, whole/London; cells = precision LIFT vs that context's OWN chance with ▲/▼/· markers and hits/fires; union coverage bottom row; pending/disabled one line) and page-2 per-hypothesis cards (claim sentence, session x window grid, payoff breakdown, dated exhibits); full detail lives in signal_scoreboard.json. Reading guide rewritten to match. (b) PER-CONTEXT BASE RATES fixed everywhere (the real computation gap in the order): every session/window context now computes its OWN either-direction AND per-direction chance rates (vectorized session masks) — session cells were previously uninterpretable. CONSEQUENCE, recorded: under honest per-context chance, H2's backtest lift is +1.2pp (was read as ~+3pp against the approximated chance) — inside the at-chance band; its promote-candidate note amended same day; label unchanged (no decisions this cycle). (c) SMALL-N CONVENTION registered (operator numbers): fires<20 or episodes<10 -> dimmed (°...) and EXCLUDED from any future label arithmetic. Marker band ±2pp registered as presentation convention, implementer-proposed, ratification pending. (d) SIGNAL PAYOFF registered (directional rows only; yardstick signal_payoff_window = the move-definition +60 min, changeable only by re-registration; standard-horizon marks in the JSON): per fire, signed excursion at +60 min signed by predicted direction; reported as total right / total wrong / net / median-per-fire; guide caveats — clustered fires double-count shared travel (median is the robust companion), mid-price, no spread, idealized; either-direction rows n/a by construction. FIRST PAYOFF READOUT (texture, no decisions): H9 the strongest line (backtest net +319, median +3.5); H2 backtest net +781 but forward net −60 DESPITE +10pp precision lift (right more often, wrong fires cost more); H7 net +7 on a +7.6pp lift (precision without payoff — clustering + loss asymmetry); H10 net −240/−220 both windows, consistent with its deprioritize recommendation.

39. **H11 + H12 registered (operator-originated 2026-08-19; register + pre-registration, NO BUILDS).** H11 — volume-at-price: low-volume zones traverse faster than baseline, high-volume nodes stall/reverse above baseline; behavioral/either-direction grading; parameters (price bucket size, rolling lookback, node/gap thresholds) MUST be pre-registered before any computation; noted as candidate conditioner for existing signals (H6 location thread; register-16 backlog #2 volume-profile sense-organ = the natural instrument). H12 — absorption-sequence zones: repeated visits + elevated volume + diminishing range-per-unit-volume + drying pullback volume -> directional move away in the absorber's direction; directional grading; the multi-visit COMPOSITE of H3's single-cluster claim (cross-referenced); sequence parameters (min visits, window, drying/diminishing definitions) to pre-register. ORIGIN recorded in both entries: operator volume-profile question + institutional-execution thought experiment 2026-08-19, with the fund-execution reasoning as the causal story (nodes = standing institutional interest; gaps = undefended prices; absorption = inventory-building that caps range per unit volume until opposing flow dries). Both DEFINITION-PENDING until parameters ratified; scoreboard rows begin when conditions are mechanical (register-35 enforcement applies automatically). Register now H1-H12.

40. **Multi-instrument expansion — three pairs registered, collection live, validation staged (operator order 2026-08-19).** (a) PAIRS per the uk100/uk100fut template: ger40 (17068) + ger40fut (70088); nas100 (20190, slug RENAMED from ustech100 — stores git-mv'd, history intact, manifest keys renamed) + nas100fut (70433); us30 (17322) + us30fut (70153). Sessions in NATIVE timezones: ger40/ger40fut moved to Europe/Berlin 09:00-17:30 (DST-proof; wall-clock identical to the old London span in aligned months); nas100/nas100fut/us30fut on America/New_York 09:30-16:00. GOLD and EURUSD examined-and-PARKED with reasons in DATA.md (gold: no rolling-future/real-volume feed visible at provider; EURUSD: spot volume synthetic, futures volume a minority shadow of an OTC market); revisitable by re-registration. (b) COLLECTION LIVE: the collector's INSTRUMENTS map is the config-driven list (sync_daily already runs --instr all); first sync 2026-08-19 12:22Z — all six feeds landed, 182/182 store checks, the retention clock stopped bleeding on all three pairs with ~30 days of 1M history captured. Collection only; NOTHING feeds any study. (c) FIRST-FETCH SANITY (computed, per pair): ~31k joint 1M bars each; basis median +0.42%/+0.32%/+0.27% (nas/dax/dow) with stable p5/p95 bands — carry-sized, tracking; futures volume real (median 463/133/103 per bar, 0.0% zero-volume bars). NOTHING FLAGGED. (d) STAGED VALIDATION, one instrument-evening each, priority nas100 -> ger40 -> us30, tracked per-instrument HERE as amendments to this entry: (i) feed probe per the measured-contract template — the FTSE index-0/immutability findings are NOT inherited; (ii) volume provenance verdict per pair, verified not assumed; (iii) session-boundary measurement against the market's native calendar INCLUDING its own pause windows (the finding-24 lesson applies per market); (iv) store verification in the daily run. FENCE: no instrument's data feeds any study, scoreboard row, or census until its validation evening passes. (e) SCOPE: data expansion only — no hypothesis, engine, or scoreboard changes ride this; cross-instrument studies are future registrations; the sealed-window schedule applies from each instrument's first bar (is_sealed is instrument-agnostic by construction).

**Queue rulings (operator, 2026-08-19; register 40 companion).** (6) Sense-organ order RE-RANKED: volume profile (organ #2) now builds before velocity/efficiency (#1) — two registered customers (H11 + the H6 thread) vs one (H12); one-at-a-time discipline unchanged; item 22 remains the active slot ahead of both. (7) Observer Cockpit confirmed FREE TO INTERLEAVE per its existing authorization (touches no evidential path) — may start whenever slack exists, consuming no queue position. (8) RESTART-EVENING PAIRING noted for scheduling: the item-22 deployment restart and the post-close thin-tape probe share one supervised evening (the probe carries two loads: latency reclaim + every Asia-session cell's interpretability); evening TBD by operator.

41. **Recipe layer v0 (operator order 2026-08-19): grammar + versioned sets, values deferred to ratification.** (a) GRAMMAR built (backtest/recipes.py): stops = fixed points | k x ATR15 | trailing beyond the Nth previous settled 1M bar's extreme +/- offset (points or k x ATR, RATCHET ONLY); targets = fixed | k x ATR | none (trail-out); any composition. Entry = next 1M bar open after a directional fire; one position per hypothesis per instrument; measured median cash spread charged per trade per instrument. (b) VERSIONED SETS: recipe_set_v0 registered (trial-log-counted; registry-flagged ILLUSTRATIVE awaiting ratification): R-FIX 10/20, R-ATR 1.5x/3.0x, R-TRAIL 6th-bar, R-TRAIL-ATR 6th-bar +/- 0.5xATR; artifacts state their set version; changes only by re-registration. (c) HONEST-FILL RULES (operator-ruled, printed in every artifact header, EACH PINNED AS A TEST): both-reachable -> stop always; gap -> filled at the gapped price; trailing ratchets only on settled bars; EOD flat at native session close. Twin-run bit-identity pinned. (d) OUTPUT: reports/scoreboard/recipe_performance.md — page-0 points rollup (hypotheses x sessions + total) per recipe per instrument per window; recipe-comparison line per hypothesis (same fires, four harvests side by side); cards detail in the JSON. FENCES: observational only (paper/ledger/frozen_v1/labels/criteria untouched); engine hash embedded; PROVISIONAL stamps carried for the three unvalidated instruments; sealed windows + lockbox excluded. (e) FIRST RUN (illustrative values — texture for the ratification discussion, NO readings): uk100fut mostly net-negative across recipes (consistent with the signal layer's at-chance home readings); scattered large positive PROVISIONAL cells on nas100 (small-n). DESIGN OBSERVATION recorded: fixed-POINT recipes do not scale across instruments (10 pts = 0.10% FTSE / 0.03% NASDAQ) — v1 wants per-instrument points or ATR-only. (f) TWO PRE-REGISTERED QUESTIONS from the exploratory cross-instrument read (trial log, expectations dated 2026-08-19): Q-H1-GEN (H1 positive lift on the three new instruments' forward windows — prediction YES) and Q-H7-GEO (H7's European-vs-US split persists forward — prediction YES); both graded ONLY on forward accrual + post-validation data at the first review where each instrument holds >=30 forward episodes post-validation. (g) Candidate-guard IMPORT_ALLOWED extended to backtest/recipes.py (observational reader, same class as the scoreboard).

42. **Recipe dispositions + grammar v1 + R-OP1 + excursion study (operator order 2026-08-19).** (1) R-TRAIL DEGENERACY logged as a design finding (verified from the artifact): win rates 3.6-13.3%, medians ~= -1.3 = exactly -median spread — the offsetless 6th-bar trail sits inside entry noise and stops out within minutes at breakeven-minus-spread; a trail without an offset or arming stage is a spread donation machine. (2) R-ATR N-COLLAPSE logged: no time exit -> occupancy blocking -> n 20-51 (uk100 backtest; lower elsewhere), variance-dominated. (3) RECORDED OBSERVATION, not minted: nas100-H9-R-ATR positive both windows (+940/20 bt, +361/13 fwd) — one cell in an exploratory grid; revisit at the first forward review. (4) VALUE-PROVENANCE registered: v0's R-FIX/R-ATR/R-TRAIL values originated as operator examples in the design discussion, never ratified; R-TRAIL-ATR's 0.5x offset was a reviewer invention with no basis. Provenance now printed per recipe in every artifact. (5) GRAMMAR v1 (all five extensions, each test-pinned): (a) initial+trail COMPOSITION — effective stop = tighter-of, ratchet-only, settled bars, honest fills unchanged; (b) ATR TF DECLARED per component (any ladder rung; no implicit default — normalization REFUSES undeclared; v0 retro-annotated 15M); (c) trail bar-reference TF declared (management TF), stop evaluation cadence 1M always; (d) STAGES with transition conditions (progress-arming); breakeven expressible with the literature-adverse caution registered HERE: breakeven moves are the most-cited way retail management converts winners to scratches — any breakeven stage carries this note into its artifact; partial exits deferred; (e) NARRATIVE-CONDITION PRIMITIVES (opposing structural core / trend flip / phase transition / opposing S-H fire) declared in signal_watch per the one-home rule — CAPABILITY ONLY, grammar-validated, synthetic-tested; no narrative recipe before item 7b reported. (6) R-OP1 registered (first ratified-provenance recipe; ATR TF ASSUMED 15M — flagged for operator statement; 5.0pt offset instrument-absolute — ATR-relative is the operator's later call) and run across all hypotheses x instruments x windows in set v0.1: at home it lands mid-pack and net-negative like everything else (e.g. H2 bt -185 on 182 trades vs R-TRAIL's -547) — the composition mechanically rescues the degenerate trail but no recipe turns at-chance signals positive, which is the honest expectation. (7) EXCURSION STUDY pre-registered and FIRST-RUN (reports/scoreboard/excursion_profiles.md|.json): MFE/MAE/time-to-MFE distributions per hypothesis x instrument x session, counts everywhere; NARRATIVE CUT first readout — the gate question answers YES FOR EXACTLY ONE CLASS: opposing 1M TREND FLIP during the hold marks adverse resolution 53.6% vs 26.9% without (n=679/201, uk100 backtest); opposing structural cores are SATURATED (870/880 windows — useless as a filter at 1M print frequency); opposing signal fires barely split (48.4/45.0). The trend-flip-against primitive is therefore the sole candidate for a narrative-exit ratification discussion; the other two fail their own gate on first read. NO recipe changes flow without ratification. (8) Component map added to README; recipe registry entries verified citing file:line. (9) WEEKLY RUNS continue on v0.1 (v0 four + R-OP1), provenance-stamped; the v1 values discussion convenes on the excursion tables, now landed.

43. **Post-flip continuation cut (pre-registered, run 2026-08-19; the flip-exit ratification gate).** Built backtest/flip_cut.py per the pre-registration: (a) further adverse excursion after flip confirmation (distributions per hypothesis x instrument); (b) counterfactual on the SAME trades — exit-at-flip (settled flip-bar close, spread both legs) vs hold-to-rule under each v0.1 recipe; flips confirming on the exit bar excluded (boundary convention pre-registered). GATE READOUT — TWO READINGS, recorded not resolved (the pre-registration's "winning on real n" did not disambiguate; per register-29 discipline the ambiguity is flagged rather than resolved toward convening): PER-TRADE, exit-at-flip improves the majority of flip-marked trades in ALL EIGHT instrument-windows (60.9-68.2% flip-wins; n=513 home backtest); NET POINTS, it LOSES at home (uk100 -424/-80) and ger40, wins on nas100, splits us30. MECHANISM: the flip cuts losers early but amputates the winners' fat right tails (home: H10/H2/H9 improved +55/+33/+94; H7/H1 destroyed -216/-84) — a good median manager, a bad tail manager. Convening the narrative-exit values discussion is the OPERATOR'S call on which reading the gate meant; any flip-exit rule would in any case need tail protection (e.g. flip-exit only before progress arms, staged grammar already expresses this). No recipe changes without ratification.

44. **Flip-gate ruling + R-FLIPGUARD (operator, 2026-08-19).** (1) GATE RULED NOT PASSED for a universal flip-exit: NET POINTS is the deciding currency (resolves register 43's recorded ambiguity); home Δnet negative both windows; mechanism on record — median manager, tail amputator (H7 −216 / H1 −84 at home; nas-class tail losses cited) — incompatible with the excursion finding that only tails are harvestable. No universal narrative-exit values discussion convenes. (2) FORBIDDEN MOVE registered: applying flip-exits only to rows where the flip-cut tables favored it is post-hoc fitting on tiny cells — barred. (3) R-FLIPGUARD registered (counted, recipe_set_v0.2): the ONE staged narrative candidate — flip-exit stage active ONLY BEFORE the progress-arming transition; R-OP1's stop composition in both stages; arming = 1.0×ATR(15M) progress OR 45 min age (whichever first), values DERIVED-STATED from the early-flip adversity (7/8 contexts) and t-MFE (13–45 min) findings, NOT the counterfactual cells; grammar gained the progress_or_age transition (test-pinned both ways: pre-arm flip exits, post-arm flip ignored); graded on forward accrual alongside R-OP1, same fences; RATIFICATION BEFORE ANY STATUS. (4) flip_cut ARCHIVED as the standing reference for why narrative exits must be stage-scoped — the ruling banner is in the generated artifact's header (persists on regeneration); weekly runs unchanged (now v0.2).

**Deployment note (2026-08-19; corrects the register-40 companion ruling 8 pairing).** The paper process was cleanly restarted 2026-08-19 12:37:14Z (STOP with last_processed) -> 12:38:18Z (START, 3-min gap) — ledger-verified. CONSEQUENCES: the finding-24 watchdog UTC calendar and the finding-26 SUSPENSION_GAP detection are NOW LIVE in the running process; finding 24's "nightly ~21:10-22:03Z false stalls expected until restart" note is OBSOLETE from tonight — a false stall tonight would be a NEW finding, not expected texture. The thin-tape probe evening is STANDALONE (~50 min); item 22's future deployment restart is its own separate event (it still carries the session-JSONL build and the scoreboard live attachment).

45. **Backlog activation (operator order 2026-08-20): every registered idea testable or explicitly decision-blocked.** (1) BUILD ORDER RULED: the volume-profile organ started ahead of item 22 (which retains the slot immediately behind). Organ BUILT reader-side same day (backtest/volume_profile.py — engine field emission deferred, no engine contact; working set only): bucket 4.0 pts (= measured median 1M true range), lookback K=5, node/gap = p90/p10 of the trailing profile (top decile carries 20.5% of volume). HONESTY FLAG in the artifact and the H11 proposal: next-session profile rank-correlation peaks at ~0.13 and turns negative past K=5 on 16 sessions — profile persistence is MARGINAL; K=5 is best-of-a-weak-field; H11's premise is itself what the row will test. Proposals EMITTED for ratification: H11 parameters (organ-derived) and the location thresholds for the session-extreme-rejection entry (measured day-relative spread p90 + founding tolerances cited); both hypotheses signal-live ON ratification. (2) H12 PARAMETER PROPOSAL logged (no build needed): visit = zone-band entry (0.25xATR founding tolerance), min visits 3, window 90 min, diminishing = strictly decreasing per-visit median range/volume, drying = pullback rel_volume < 0.7 (founding low_volume_mult) and each pullback quieter; fire direction = higher-volume approach side, mechanical. (3) H5 REVIVE-AS-NEW (operator decision): bare condition DRAFTED from founding anatomy (structural BUYING_CLIMAX with close - SMA20(1M) >= 2.0 x ATR(1M), short-only; founding h5 values cited; TF interpretation flagged); origin recorded founding-anatomy/fresh-start; signal-live on ratification. (4) CO-FIRE CENSUS pre-registered then run (backtest/cofire.py; reports/scoreboard/cofire.md|.json): 24 ordered cross-family pairs x {5,15} min windows x 4 instruments x both windows; family partition lives in signal_watch (one-home rule — the guard caught and forced BOTH this and the organ's identifier mentions during the build, third and fourth enforcement instances). FIRST READ: almost every with-cell is small-n or unmeasurable (the coverage arithmetic bites exactly as the order predicted — the with-stack is bounded by the rarer leg); the few measurable cells sit within noise of their without-cells; NO pair earns a composite registration on this read; the census reruns weekly and any future real lift goes through the H13+ front door. (5) docs/backlog_status.md CREATED — every idea carries exactly one status (testable-now / awaiting-ratification-of-X / awaiting-build-Y / awaiting-decision-Z); linked from the queue page. Standing-ratification list consolidated at the bottom: one operator sitting clears eight pending words.

46. **Ratification sitting, part 1 (operator, 2026-08-20).** RATIFIED: (1) H5 as drawn, anatomy read on the SIGNAL TF (15M) per founding origin — implemented as an event-derived row (S-H5: 15M structural BUYING_CLIMAX with close − SMA20(15M) ≥ 2.0×ATR(15M), short); a 1M variant, if ever, is a separate hypothesis. (2) H6 thresholds as proposed (0.25×ATR(15M) proximity; day-relative spread ≥ p90; close_pos 0.25/0.75; wick 0.33; volume-agnostic) → S-H6 live; the 480-bar day-relative window is an IMPLEMENTER CONSTANT, registry-flagged operator-adjustable. (3) H11 as proposed, the weak-persistence measurement standing in the entry as the premise's own first stress → S-H11 live (TRAVERSAL clause only; the node-stall clause needs its own grading mode — pending sub-question; home-derived bucket carries a mis-scale caveat on other instruments). (4) H12 as proposed → S-H12 live. (5) R-OP1 ATR TF = 15M CONFIRMED (assumption → ratified value). (6) R-FLIPGUARD arming values ratified as derived-stated. ALL TWELVE register entries are now signal-live — the board is complete. FIRST READOUTS (home backtest): **S-H6 opens at 45.8% (27/59) vs 23.6% chance (+22pp) — the strongest backtest row yet printed, and therefore SUSPECT per the beauty tripwire: H6 conditions on p90-wide bars, which mark high local volatility; its FIRST REVIEW QUESTION is registered as: does the lift survive a wide-bar-conditioned chance baseline?** S-H11 +2.3pp (n=316, marginal). S-H5/S-H12 open 0 fires (rare by construction). Guard note: the ratification build tripped the identifier guard twice more; event-row names now live as named constants in the signal module.

47. **Scoreboard fairness amendment — class-conditioned chance (operator order 2026-08-20).** RULE: for any hypothesis whose firing condition conditions on a bar-size/volatility class, the scoreboard computes the class's own chance rate per context, prints it beside the whole-window rate, and grades the row's lift against the CONDITIONED one. **Unconditioned lift on a conditioned signal is a known confound class; the scoreboard refuses to display it as headline lift.** Declarations live in the signal module (CONDITIONED_ROWS, one-home rule); mask implementations in the scoreboard, name-keyed; matrix and card cells show "[cls-chance X% vs uncond Y%]" and mark/grade from the conditioned rate. APPLIED TO S-H6 (class = its own ratified p90-width condition over the trailing day window) and re-emitted: wide bars alone qualify at 37.8% (backtest) vs 23.6% unconditioned — the volatility confound explained ~14pp of yesterday's +22pp headline; HONEST CONDITIONED LIFT: +8.0pp backtest (27/59), +11.5pp forward (6/11, small-n). Above its class's chance at modest n; keep-watching stands; the register-46 review question is RESOLVED into this standing column.

**Dated observation (operator, 2026-08-19; artifact-verified and registered 2026-08-20; NO question minted; schema-v2 annotation 2026-08-20: this observation cites S0-H2 specifically — the bare-anatomy signal).** H2 forward, two layers absent from every backtest window: directional precision lift positive on all four instruments (+4.0 uk / +10.0 ger / +6.6 nas / +4.2 us30 pp, n 87-141) while backtest sits at/below chance on all four; and R-ATR forward nets positive on all four (+121/+33/+177/+287 pts, n 10-19). Noise, regime (August vs July tape), and reality all live. Revisit at the first >=30-post-validation-episode review alongside Q-H1-GEN and Q-H7-GEO; a question is minted then only if the pattern persists. H2's promote-candidate recommendation is UNCHANGED by this note (it already cites the forward strength; the backtest weakness note from register 38 also stands).

48. **Review day (operator order 2026-08-20; outputs = candidate lists and findings, NOTHING auto-minted).** Full document: reports/review_2026-08-20.md. (1) COMPLETENESS SWEEP: twelve articulated-but-unregistered ideas compiled with source/date/suggested claim for operator yes/no — headliners: contra-direction prints differential (parked curiosity, 08-17), rung-consensus composition (cascade candidates, 08-17), the modifier reframe (08-18), the R2 spread-vs-refinement question (now ANSWERABLE by the recipe machinery), and a proposed explicit retirement of the overtaken multi-instrument-acceleration parked decision. (2) COVERAGE GRID emitted; the two operator-named holes CONFIRMED and one more found: session-boundary behavior (entirely unowned), volatility-regime TRANSITIONS/coil side (no claim; organ #3 is its instrument), and participation-shift as a firing condition (H9's recruitment clause — see F3). (3) LOCATION CENSUS (pre-registered, run): the exhaustion family is itself starved — every cell unmeasurable (home: H3 6 fwd / H4 7 bt / H12 0 fires); the seller-exhaustion-at-location question accrues weekly; no candidate emerges. (4) AUDIT, three passes: **(a) claim-vs-code, three findings — F1: H1's "late in an extended move" is NOT in the row (structural core lacks trend-extension; qualified-layer-only clause); F2: H2's "beyond a prior extreme" is NOT in the row (structural upthrust/spring is pure bar anatomy); F3: H9's falsifiable recruitment clause is absent from its firing condition (row fires pooled depth>=2)** — each needs operator disposition (trim claim vs extend condition; F3: dual-row vs annotation); every change via re-registration. (b) doc-consistency: F4-F7 (stale lifecycle stage 4 pre-doctrine text; stale backlog statuses; component map missing five modules; queue-page staleness) — ALL FIXED same day, doc-drift class. (c) INVARIANT INVENTORY: pinned list vs six assumed-unpinned properties with proposed pins P1-P6 (EOD-DST behavior, _atr15 equivalence, unit fixtures for the three ratified condition classes, S-H5 arithmetic, class-mask and trailing-profile no-lookahead) — build on operator nod. Evidence Regression applies to any accepted finding's blast radius when its disposition lands.

49. **Register schema v2 + audit dispositions + identifier ruling (operator, 2026-08-20; supersedes the earlier "dual rows" disposition block).** (1) SCHEMA: hypothesis H<n> = claim + origin + status; signal S<k>-H<n> = one mechanical configuration (condition, parameters, provenance, date, status); many signals per hypothesis, one hypothesis per signal; global yardsticks shared and outside both. (2) NAMESPACE: H<n> and S<k>-H<n> the ONLY identifier forms; S-numbers per-hypothesis, never reused; no in-place parameter edits — changes are new S-numbers (append-only configuration history by construction); -ctx suffixes, lab serials, descriptive names banned (serials survive as cross-references only). Validation refuses everything else — pinned with the old form, a serial, a suffix, and a free name. (3) MIGRATION complete: all live rows -> S0-H<n> with history attached under accurate labels; scoreboard renders signals grouped under hypotheses with configuration counts visible; code/register/docs/JSON migrated. (4) F1/F2/F3 RESOLVED under the schema: S0-H1/S0-H2 labeled anatomy-only (claims unchanged); S1-H1 and S1-H2 = the founding context-qualified conditions, registered and live; S0-H9 labeled recruitment-agnostic (retirement flagged for next sitting, default keep); S1-H9 = recruited-only (the claim's core), live, expected starved at current n and stated so; the 2026-08-19 H2 forward observation annotated to cite S0-H2 specifically. (5) STANDING GUARDS registered: pre-registration with rationale before first computation; per-hypothesis configuration counts on every card and matrix; labels/promote-eligibility attach to SIGNALS; no best-of-configurations display — all of a hypothesis's signals print or none do; claim-implementation divergence resolves as a NEW signal under the honest claim, never a silent rewrite. (6) CARRIED: P1-P6 pins APPROVED and BUILT (tests/test_invariant_pins.py — eight tests; two pin-writing lessons caught in the writing: a bar's own range legitimately determines its class, and degenerate equal-volume fixtures make everything a gap); twelve-candidate list held for discussion; multi-instrument-split retirement accepted (README updated); R2 routed to the recipe layer as a counted experiment; location census verdict stands and accrues. (7) Scoreboard re-emitted under the schema (next block).

50. **H13 registered — value-area reclaim (via operator, external trader advice, 2026-08-20).** The register's FIRST COMPOSITE born from its own prior entries: the operator's H11/H12 origination questions composed with an externally-sourced reclaim trigger; source description recorded VERBATIM in the entry's origin field. Claim: break out of the session value area on declining volume + reclaim on expanding volume -> continuation toward the far side. S0-H13 PRE-REGISTERED BEFORE COMPUTATION then implemented: value area = central 70% band of the trailing profile (POC expansion; H11's ratified config), break/fade/reclaim per the entry; the "declining" clause's implementation reading flagged (a stricter variant = new S-number); the one new value (70%) operator-specified at registration; everything else registry-cited. SCOPE stated in the entry: the absorption clause omitted (unmeasurable without organs #4/#5 — a richer S<k> may add it); the source's stop/target geometry routed to the recipe layer (future level-based grammar extension). Expected fire rate LOW, stated. Unit-pinned (break-fade-reclaim fires toward the far edge; heavy-volume excursion refused). Signal-live; rows open at the next run, all four instruments, PROVISIONAL off-home. Register now H1-H13.
